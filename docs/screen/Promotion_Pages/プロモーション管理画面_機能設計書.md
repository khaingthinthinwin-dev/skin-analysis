# Functional Specification (機能設計書) — Promotion Management

---

## Document Control

| Attribute | Value |
|-----------|-------|
| **Document ID** | SKM-FDS-PROMO-001 |
| **Target Screen** | Promotion Management (プロモーション管理) |
| **Subsystem** | Promotion — Merchant CRUD Operations, Coupon Validation |
| **Function ID** | FN-PROMO-001 |
| **Version** | 1.5 |
| **Created** | 2026-08-17 |
| **Last Updated** | 2026-08-24 |
| **Author** | Software Architect |
| **Status** | Released (承認済み) |
| **Classification** | Internal — Engineering Division |

---

## Document Revision History

| Version | Date | Author | Description of Changes |
|---------|------|--------|------------------------|
| 1.0 | 2026-08-17 | Software Architect | Initial functional specification for Promotion Management covering merchant promotion CRUD, coupon validation, and related business rules. |
| 1.1 | 2026-08-17 | Software Architect | Aligned with REQUIREMENT_SPEC v1.5, DATABASE_SPEC v2.0, and DEVELOPMENT_RULES v2.0: UUID primary keys, comprehensive error classification, license status enforcement, audit logging, ownership enforcement, API versioning (/api/v1/), screen transitions, non-functional requirements. |
| 1.2 | 2026-08-17 | Software Architect | Added Promotion Conflict Resolution & Stacking Rules (§16) and Redis Cache Invalidation Strategy (§17) — Backend Application Logic and Redis Caching only, no new PostgreSQL tables. |
| 1.3 | 2026-08-19 | Software Architect | Currency changed to MMK (integer only, Math.floor), removed minOrderAmountAfterDiscount (0 MMK allowed), added Redis Distributed Lock, Quota Rollback, and Mutex/Singleflight (§17.6–17.9). |
| 1.4 | 2026-08-19 | Software Architect | Aligned with DATABASE_SPEC v2.2: fixed API method PUT→PATCH, fixed ownership check (user.merchantId), changed currency type from INTEGER to DECIMAL(10,2), removed `updated_at`/`deletedAt` fields not in DB schema. Removed overengineered sections: Promotion Stacking Rules (§16), Lazy Invalidation (§17.3), Distributed Lock (§17.6), Quota Rollback (§17.7), Mutex/Singleflight (§17.8). Converted logical delete to hard delete (no `deleted_at` column in DB). Simplified Redis caching strategy. Removed unused config items. |
| 1.5 | 2026-08-24 | Software Architect | Aligned promotion behavior with the current database schema and development rules: clarified Prisma field naming, merchant/admin scope, coupon validation ownership, decimal currency handling, UTC timestamps, and removed unsafe Redis quota mutation from validation. |

---

## Table of Contents

1. [Functional Overview](#1-functional-overview)
2. [Use Cases and Business Workflow](#2-use-cases-and-business-workflow)
3. [State Transition Specification](#3-state-transition-specification)
4. [Business Rules](#4-business-rules)
5. [Screen Specifications](#5-screen-specifications)
6. [Functional Operation Specification](#6-functional-operation-specification)
7. [Input / Output Specification](#7-input--output-specification)
8. [Input Validation Rules](#8-input-validation-rules)
9. [Error Handling Specification](#9-error-handling-specification)
10. [Permission and Access Control](#10-permission-and-access-control)
11. [Real-Time Notification Behavior](#11-real-time-notification-behavior)
12. [Screen Transition Specification](#12-screen-transition-specification)
13. [Non-Functional Considerations](#13-non-functional-considerations)
14. [Configurable Items (External Definitions)](#14-configurable-items-external-definitions)
15. [Cross-Reference Traceability Matrix](#15-cross-reference-traceability-matrix)
16. [Redis Cache Invalidation Strategy](#16-redis-cache-invalidation-strategy)

---

## 1. Functional Overview

### 1.1 Purpose and Scope

This subsystem manages the complete lifecycle of promotional discount codes (coupons) within the Cosmetics Finder marketplace. It provides merchants with full CRUD capabilities for promotions and enables buyers to validate and apply coupon codes during checkout.

The Promotion Management subsystem is central to the marketplace's sales strategy, enabling merchants to create targeted discount campaigns to attract buyers and increase conversion rates.

### 1.2 Functional Responsibilities

This subsystem is responsible for the following core functional areas:

1. **Promotion Creation** — Merchants can create new promotions with code, description, discount type (percentage/fixed), discount value, minimum order amount, maximum uses, and validity period.
2. **Promotion Editing** — Merchants can update existing promotion details, discount settings, and active status. **Note: Promotions that have been used (used_count > 0) cannot be edited.**
3. **Promotion Deletion** — Merchants can delete promotions (hard delete), permanently removing them from the database. **Note: Promotions that have been used (used_count > 0) cannot be deleted.**
4. **Coupon Code Validation** — Buyers can validate coupon codes at checkout, checking expiration, usage limits, minimum order amount, and active status.
5. **Usage Statistics** — Merchants can view promotion usage counts and last usage dates.
6. **License Status Guard** — Merchants with `pending` or `rejected` license status cannot create, edit, delete, or activate/deactivate promotions. The promotion list page may load in restricted read-only mode; direct mutation requests remain blocked by the backend.

### 1.3 Target Users

| Attribute | Value |
|-----------|-------|
| **Primary Actor (Merchant)** | Authenticated merchant managing their promotions (license status must be `approved`) |
| **Primary Actor (Buyer)** | Authenticated buyer validating and applying coupons at checkout |
| **Primary Actor (Admin)** | Admin with full promotion management access |
| **Restricted Actor (Pending/Rejected Merchant)** | Authenticated merchant with `pending` or `rejected` license status — promotion mutations are restricted |
| **Required Authentication** | JWT Bearer Token for merchant/buyer operations |
| **Data Scope** | Merchant: own promotions only. Buyer: validate coupons for checkout. Admin: all promotions. Pending/rejected merchant: read-only list, no mutations. |

### 1.4 Relationships with Other Functions and Peripheral Systems

```text
┌──────────────────────┐      ┌─────────────────────────────────────┐
│   Merchant Actor     │      │     promotions                      │
│   (Manages Promos)   ├─────►│  CRUD operations                    │
└──────────────────────┘      └──────────────┬────────────────────┘
                                             │ Reads/Writes
                                             ▼
                                  ┌────────────────────────┐
                                  │  Promotions Module     │
                                  └──────────┬─────────────┘
                                             │
                        ┌────────────────────┼────────────────────┐
                        ▼                    ▼                    ▼
             ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
             │  Orders          │  │  Cart / Checkout  │  │  Merchants       │
             │  (Applied Coupon)│  │  (Coupon Validate)│  │  (Owner)         │
             └──────────────────┘  └──────────────────┘  └──────────────────┘
                                             │
                                  ┌─────────────────────────────────────┐
                                  │     Redis Cache                     │
                                  │  Promotion cache, validation cache  │
                                  └─────────────────────────────────────┘
```

### 1.5 Inputs / Outputs

| Input Information | Data Category | Source / Description |
|-------------------|---------------|----------------------|
| `code` | User Input | Promotion code (alphanumeric, 1-50 chars) |
| `description` | User Input | Promotion description |
| `discountType` | User Input | 'percentage' or 'fixed' |
| `discountValue` | User Input | Discount amount or percentage |
| `minOrderAmount` | User Input | Minimum order amount for discount |
| `maxUses` | User Input | Maximum number of times code can be used |
| `startsAt` | User Input | Promotion start date/time in UTC |
| `expiresAt` | User Input | Promotion end date/time in UTC |
| `isActive` | User Input | Promotion active status |

| Output Information | Data Category | Destination / Description |
|--------------------|---------------|---------------------------|
| `promotion` | Promotion DTO | Full promotion data with usage statistics |
| `validation_result` | Validation DTO | Coupon validation result with discount calculation |

### 1.6 Related Documents

| No. | Document ID | Document Name | File Path / Reference | Remarks |
|-----|-------------|---------------|----------------------|---------|
| 1 | SKM-REQ-001 | Requirements Definition | `docs/core-work/要件定義書_REQUIREMENT_SPEC.md` | M-PROMO-001~006, promotion business rules |
| 2 | SKM-DBS-001 | Database Design Specification | `docs/core-work/データベース設計書_DATABASE_SPEC.md` | `promotions` table, Prisma mappings, CHECK constraints |
| 3 | SKM-DEV-001 | Development Rules | `docs/core-work/開発ルール_DEVELOPMENT_RULES.md` | Naming conventions, RBAC, API standards |
| 4 | SKM-FDS-PROD-001 | Product Management Spec | `docs/screen/Product_Management/商品管理画面_機能設計書.md` | Reference for spec structure and patterns |

---

## 2. Use Cases and Business Workflow

### 2.1 Use Case Catalog

| UC-ID | Use Case Name | Precondition | Postcondition | Triggering Actor |
|-------|---------------|--------------|---------------|------------------|
| UC-PROMO-001 | Create New Promotion | Merchant is authenticated and license status is `approved`. Merchants with `pending` status are restricted. | New promotion record created in `promotions` table. Promotion appears in merchant's promotion list. | Merchant |
| UC-PROMO-002 | View Merchant Promotion List | Merchant is authenticated. Approved merchants have full access; pending/rejected merchants may view the list in read-only mode. | List of merchant's own promotions displayed with mutation controls hidden when restricted. | Merchant |
| UC-PROMO-003 | Edit Existing Promotion | Merchant is authenticated. Promotion belongs to merchant. Promotion has not been used (used_count = 0). License status is `approved`. Merchants with `pending` status are restricted. | Promotion record updated. | Merchant |
| UC-PROMO-004 | Delete Promotion (Hard) | Merchant is authenticated. Promotion belongs to merchant. Promotion has not been used (used_count = 0). License status is `approved`. Merchants with `pending` status are restricted. | Promotion record permanently removed from database. | Merchant |
| UC-PROMO-005 | Validate Coupon Code | Buyer is authenticated. Cart has items. | Coupon validated. Discount calculated and displayed at checkout. | Buyer |
| UC-PROMO-006 | View Promotion Statistics | Merchant is authenticated. Promotion exists. | Usage count and last usage date displayed. | Merchant |

### 2.2 Primary Business Workflow — Merchant Promotion Management

```
                    ┌──────────────────────┐
                    │  Merchant Logs In    │
                    │  (JWT Authenticated) │
                    └──────────┬───────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │  License Status      │
                    │  Check               │
                    └──────────┬───────────┘
                               │
                ┌──────────────┴──────────────┐
                ▼                             ▼
     ┌──────────────────┐          ┌──────────────────┐
     │ Status = pending │          │ Status = approved│
     │ → Show Error     │          │ → Proceed to     │
     │   Toast          │          │   Dashboard      │
     │ → Redirect to /  │          └────────┬─────────┘
     └──────────────────┘                   │
                                            ▼
                                 ┌──────────────────────┐
                                 │  Merchant Dashboard  │
                                 │  /merchant/promotions│
                                 └──────────┬───────────┘
                                            │
                               ┌────────────────┼────────────────────┐
                               ▼                ▼                    ▼
                    ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
                    │ View Promo      │ │ Add New Promo   │ │ Edit Promo      │
                    │ List            │ │ (UC-PROMO-001)  │ │ (UC-PROMO-003)  │
                    └────────┬────────┘ └────────┬────────┘ └────────┬────────┘
                             │                   │                   │
                             ▼                   ▼                   ▼
                    ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
                    │ Filter/Search   │ │ Fill Promo      │ │ Update Fields   │
                    │ Promotions      │ │ Form            │ │ & Save          │
                    └────────┬────────┘ └────────┬────────┘ └────────┬────────┘
                             │                   │                   │
                             │                   ▼                   │
                             │          ┌─────────────────┐          │
                             │          │ Submit Promo    │          │
                             │          │ (UC-PROMO-001)  │          │
                             │          └────────┬────────┘          │
                             │                   │                   │
                             │                   ▼                   ▼
                             │          ┌─────────────────┐ ┌─────────────────┐
                             │          │ Promo Created   │ │ Promo Updated   │
                             │          │ in Database     │ │ in Database     │
                             │          └─────────────────┘ └─────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ Delete Promo    │
                    │ (Hard Delete)   │
                    │ (UC-PROMO-004)  │
                    └─────────────────┘
```

### 2.3 Workflow Critical Path Summary

| Step | Action | Status Before | Status After | Assigned To |
|:----:|--------|---------------|--------------|-------------|
| 1 | Merchant navigates to /merchant/promotions | Authenticated | — | Merchant |
| 2 | System checks license status | — | Status verified | System |
| 3 | If status = pending or rejected → load read-only list and display status banner | — | Restricted list displayed | System |
| 4 | If status = approved → proceed to promotion list with full controls | — | List displayed | System |
| 5 | Merchant clicks "New Promotion" | — | Form Displayed | System |
| 6 | Merchant fills promotion form | — | — | Merchant |
| 7 | Merchant submits promotion | — | Promotion Created | System |
| 8 | Promotion appears in merchant list | — | — | System |

### 2.4 Relevant Requirements Covered

| Requirement ID | Requirement Summary |
|----------------|---------------------|
| M-PROMO-001 | Merchant can create, edit, delete promotions |
| M-PROMO-002 | Promotion supports percentage and fixed discount types |
| M-PROMO-003 | Promotion can set minimum order amount |
| M-PROMO-004 | Promotion can set maximum usage count and expiration date |
| M-PROMO-005 | Promotion usage statistics are displayed |
| M-PROMO-006 | Promotion can be edited and deleted by merchant |

---

## 3. State Transition Specification

### 3.1 Promotion States

| State | Description | Can Edit | Can Delete | Can Validate |
|-------|-------------|:--------:|:----------:|:------------:|
| `ACTIVE` | `isActive = true` and current time is within the validity period | ✓ (if usedCount = 0) | ✓ (if usedCount = 0) | ✓ |
| `INACTIVE` | `isActive = false` | ✓ | ✓ (if usedCount = 0) | ✗ |
| `EXPIRED` | Current time is at or after `expiresAt` | ✓ | ✓ (if usedCount = 0) | ✗ |
| `MAX_USES_REACHED` | `maxUses` is set and `usedCount >= maxUses` | ✗ | ✗ | ✗ |

### 3.2 Promotion Lifecycle Transitions

| Transition ID | Origin State | Target State | Trigger Action | Guard Conditions |
|---------------|--------------|--------------|----------------|------------------|
| TR-PROMO-01 | — | `ACTIVE` | Create promotion (default) | Valid promotion data, merchant authenticated, license approved |
| TR-PROMO-02 | `ACTIVE` | `INACTIVE` | Merchant toggles is_active to false | Promotion belongs to merchant |
| TR-PROMO-03 | `INACTIVE` | `ACTIVE` | Merchant toggles is_active to true | Promotion belongs to merchant |
| TR-PROMO-04 | `ACTIVE` | `EXPIRED` | Time passes expires_at | Automatic system check |
| TR-PROMO-05 | `ACTIVE` | `MAX_USES_REACHED` | used_count reaches max_uses | Automatic on order completion |

### 3.3 Coupon Validation States

| Validation State | Description | Can Apply |
|-----------------|-------------|:---------:|
| `VALID` | All checks passed | ✓ |
| `EXPIRED` | Current time > expires_at | ✗ |
| `INACTIVE` | is_active = false | ✗ |
| `MAX_USES_REACHED` | used_count >= max_uses | ✗ |
| `MIN_ORDER_NOT_MET` | Order amount < min_order_amount | ✗ |
| `NOT_FOUND` | Code does not exist | ✗ |

---

## 4. Business Rules

### 4.1 Promotion Creation Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROMO-001 | Required Fields | Promotion must have: code, description, discount_type, discount_value, starts_at, expires_at. | Backend (DTO validation) + Frontend (Zod schema) |
| BR-PROMO-002 | Code Uniqueness | Code must be globally unique across all merchants. Per DATABASE_SPEC, `code` has a global UNIQUE constraint (`uq_promotions_code`). | Backend (DB constraint `uq_promotions_code`) |
| BR-PROMO-003 | Discount Positive | discount_value must be > 0 (DECIMAL(10,2)). | Backend (DB constraint `chk_promotions_discount_value`) |
| BR-PROMO-004 | Percentage Range | When discount_type = 'percentage', discount_value must be between 1 and 100. | Backend (DTO validation) |
| BR-PROMO-005 | Fixed Discount Limit | When discount_type = 'fixed', discount_value must not exceed order total. | Backend (validation at checkout) |
| BR-PROMO-006 | Date Range Valid | expires_at must be after starts_at. | Backend (DB constraint `chk_promotions_dates`) |
| BR-PROMO-007 | Default Status | New promotions default to `is_active = true`. | Backend (DB default) |
| BR-PROMO-008 | Default Usage Count | New promotions start with `used_count = 0`. | Backend (DB default) |

### 4.2 Promotion Edit Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROMO-009 | Usage Restriction | Promotions with used_count > 0 cannot be edited. | Backend (service logic) |
| BR-PROMO-010 | Ownership Required | Merchants can only edit their own promotions. | Backend (service check) |
| BR-PROMO-011 | Admin Override | Admins can manage all promotions. | Backend (RBAC) |

### 4.3 Promotion Deletion Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROMO-012 | Hard Delete | Delete permanently removes the promotion record from the database. | Backend (service logic) |
| BR-PROMO-013 | Ownership Required | Merchants can only delete their own promotions. | Backend (service check) |
| BR-PROMO-014 | Usage Restriction | Promotions with used_count > 0 cannot be deleted. | Backend (service logic) |

### 4.4 Coupon Validation Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROMO-015 | Expiration Check | Current time must be between starts_at and expires_at. | Backend (validation service) |
| BR-PROMO-016 | Usage Limit Check | used_count must be less than max_uses (if max_uses is set). | Backend (validation service) |
| BR-PROMO-017 | Minimum Order Check | Order amount must be >= min_order_amount (if min_order_amount is set). | Backend (validation service) |
| BR-PROMO-018 | Active Check | is_active must be true. | Backend (validation service) |

### 4.5 Merchant License Status Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROMO-019 | Restricted License Status | Merchants with license status `pending` or `rejected` may view their promotion list in read-only mode, but cannot create, update, delete, or toggle promotions. | Backend guard + frontend conditional rendering |
| BR-PROMO-020 | Restricted License UI | The list displays a pending or rejection banner and hides mutation controls. Direct mutation requests return `403`; no client-side redirect is required. | Backend guard + frontend restricted view |

---

## 5. Screen Specifications

### 5.1 Screen: Merchant Promotion List (`/merchant/promotions`)

**Purpose:** Allow merchants to view and manage their promotions.

#### 5.1.1 UI Elements

**Action Bar:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-01 | Add Promotion Button | Button (primary) | `merchant.promotions.addNew` | Yes | Navigate to promotion creation form |
| EL-02 | Search Input | Input (text) | `merchant.promotions.search` | No | Search within own promotions by code |
| EL-03 | Status Filter | Select | `merchant.promotions.status` | No | Filter by active/inactive |

**Promotion Table:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-04 | Code | Text | — | Yes | Promotion code (e.g., SALE10) |
| EL-05 | Description | Text | — | Yes | Promotion description |
| EL-06 | Discount Type | Badge | — | Yes | percentage / fixed |
| EL-07 | Discount Value | Text (currency/percent) | — | Yes | Discount amount or percentage |
| EL-08 | Usage | Text (count) | — | Yes | used_count / max_uses |
| EL-09 | Expiry Date | Text (date) | — | Yes | expires_at |
| EL-10 | Status Badge | Badge | — | Yes | Active/Inactive/Expired status |
| EL-11 | Edit Button | Button (ghost) | — | Yes | Navigate to edit form (disabled if used) |
| EL-12 | Delete Button | Button (ghost, danger) | — | Yes | Hard delete with confirmation (disabled if used) |
| EL-13 | Toggle Active | Switch | — | Yes | Toggle promotion active status |

**Default State:**
- Promotions sorted by newest first
- All promotions shown (active and inactive)
- Expired promotions highlighted with warning badge
- Edit button disabled when used_count > 0
- Delete button disabled when used_count > 0

### 5.2 Screen: Promotion Form (`/merchant/promotions/new` and `/merchant/promotions/:id/edit`)

**Purpose:** Allow merchants to create or edit promotion listings.

#### 5.2.1 UI Elements

**Promotion Form:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-14 | Code | Input (text) | `merchant.promotions.code` | Yes | Promotion code (alphanumeric, max 50) |
| EL-15 | Description | Textarea | `merchant.promotions.description` | Yes | Promotion description (max 500) |
| EL-16 | Discount Type | Select | `merchant.promotions.discountType` | Yes | percentage / fixed |
| EL-17 | Discount Value | Input (number) | `merchant.promotions.discountValue` | Yes | Discount amount or percentage |
| EL-18 | Minimum Order Amount | Input (number) | `merchant.promotions.minOrderAmount` | No | Minimum order amount for discount |
| EL-19 | Maximum Uses | Input (number) | `merchant.promotions.maxUses` | No | Maximum times this code can be used |
| EL-20 | Start Date | Input (datetime) | `merchant.promotions.startsAt` | Yes | Promotion start date/time |
| EL-21 | Expiry Date | Input (datetime) | `merchant.promotions.expiresAt` | Yes | Promotion end date/time |
| EL-22 | Is Active | Switch | `merchant.promotions.isActive` | Yes | Promotion active status toggle |

**Form Actions:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-23 | Save Button | Button (primary) | `common.save` | Yes | Submit promotion form |
| EL-24 | Cancel Button | Button (ghost) | `common.cancel` | No | Return to promotion list |

**Default State (Create):**
- Code input auto-focused
- Is Active switch ON by default
- Start date defaults to current date/time

**Default State (Edit):**
- All fields populated with existing promotion data
- Save button text changes to "Update"
- Code field disabled (cannot be changed after creation)

---

## 6. Functional Operation Specification

### 6.1 Operation: Create Promotion (Merchant)

| Attribute | Specification |
|-----------|---------------|
| **Trigger** | Submit promotion form on `/merchant/promotions/new` |
| **API Endpoint** | `POST /api/v1/promotions` |
| **Request Content-Type** | `application/json` |
| **Pre-Submission Validation** | Full DTO validation (class-validator), merchant license status check |
| **Processing Steps** | 1. Validate JWT token and merchant role. 2. **Check merchant license status. If status is `pending` or `rejected`, return 403 Forbidden with the corresponding merchant-status error code.** 3. Verify merchant has an approved shop. 4. Validate all required fields. 5. Check code uniqueness globally. 6. Validate date range (`expiresAt` > `startsAt`). 7. Validate discount constraints. 8. Create the promotion record in the database. 9. Write the audit event. 10. Invalidate the merchant promotion-list cache. 11. Return the created promotion DTO. |
| **Success Response** | 201 Created with promotion data |
| **Error Response (Pending License)** | 403 Forbidden — "Your account is pending approval. Promotion management is not available at this time." |
| **Post-Action** | Redirect to merchant promotion list |

### 6.2 Operation: Update Promotion (Merchant)

| Attribute | Specification |
|-----------|---------------|
| **Trigger** | Submit promotion form on `/merchant/promotions/:id/edit` |
| **API Endpoint** | `PATCH /api/v1/promotions/:id` |
| **Request Content-Type** | `application/json` |
| **Pre-Submission Validation** | Full DTO validation, promotion ownership check, merchant license status check, usage check |
| **Processing Steps** | 1. Validate JWT token and merchant role. 2. **Check merchant license status. If status is `pending` or `rejected`, return 403 Forbidden with the corresponding merchant-status error code.** 3. Verify promotion belongs to the merchant. 4. **Check if the promotion has been used (`usedCount > 0`). If used, return 409 Conflict with error code `PROMO_EDIT_RESTRICTED`.** 5. Validate all fields. 6. Update the promotion record. 7. Write the audit event. 8. Invalidate promotion detail and merchant list caches. 9. Return the updated promotion DTO. |
| **Success Response** | 200 OK with updated promotion data |
| **Error Response (Pending License)** | 403 Forbidden — "Your account is pending approval. Promotion management is not available at this time." |
| **Error Response (Used Promotion)** | 409 Conflict — "Cannot edit promotion that has already been used" |
| **Post-Action** | Stay on edit page or redirect to list |

### 6.3 Operation: Delete Promotion (Hard Delete) (Merchant)

| Attribute | Specification |
|-----------|---------------|
| **Trigger** | Click delete button on merchant promotion list |
| **API Endpoint** | `DELETE /api/v1/promotions/:id` |
| **Request Content-Type** | None |
| **Pre-Submission Validation** | Promotion ownership check, merchant license status check, usage check |
| **Processing Steps** | 1. Validate JWT token and merchant role. 2. **Check merchant license status. If status is `pending` or `rejected`, return 403 Forbidden with the corresponding merchant-status error code.** 3. Verify promotion belongs to the merchant. 4. **Check if the promotion has been used (`usedCount > 0`). If used, return 409 Conflict with error code `PROMO_DELETE_RESTRICTED`.** 5. Permanently delete the promotion record from the database. 6. Write the audit event. 7. Invalidate promotion detail and merchant list caches. 8. Return 204 No Content. |
| **Success Response** | 204 No Content |
| **Error Response (Pending License)** | 403 Forbidden — "Your account is pending approval. Promotion management is not available at this time." |
| **Error Response (Used Promotion)** | 409 Conflict — "Cannot delete promotion that has already been used" |
| **Post-Action** | Remove promotion from list view, show success toast |

### 6.4 Operation: Validate Coupon Code (Buyer)

| Attribute | Specification |
|-----------|---------------|
| **Trigger** | Enter coupon code at checkout |
| **API Endpoint** | `POST /api/v1/promotions/validate` |
| **Request Content-Type** | `application/json` |
| **Pre-Submission Validation** | Buyer authentication, code format validation |
| **Processing Steps** | 1. Validate JWT token and buyer role. 2. Find the promotion by code. 3. Check `isActive = true`. 4. Check the current UTC time is within `startsAt` and `expiresAt`. 5. Check `usedCount < maxUses` when `maxUses` is set. 6. Check `orderAmount >= minOrderAmount` when configured. 7. Calculate the discount using decimal-safe arithmetic. 8. Return the validation result. **This operation must not increment usage; usage is committed atomically with the order.** |
| **Success Response** | 200 OK with validation result and discount amount |
| **Error Response** | 400 Bad Request with specific error code |
| **Post-Action** | Display discount on checkout page |

### 6.5 Operation: View Promotion List (Merchant)

| Attribute | Specification |
|-----------|---------------|
| **Trigger** | Navigate to `/merchant/promotions` |
| **API Endpoint** | `GET /api/v1/promotions` |
| **Request Content-Type** | None |
| **Pre-Submission Validation** | Merchant authentication, license status check |
| **Processing Steps** | 1. Validate JWT token and merchant role. 2. Resolve the merchant profile from the authenticated `userId`. 3. Query promotions where `merchantId` matches. Pending/rejected merchants receive a read-only list response; mutation endpoints remain protected. 4. Return the paginated promotion list. |
| **Success Response** | 200 OK with paginated promotion list |
| **Error Response (Restricted License)** | 200 OK with a read-only list and status banner; direct mutation attempts return 403 Forbidden. |

---

## 7. Input / Output Specification

### 7.1 Input Specification — Create Promotion (入力定義)

| Field | Display Name (EN) | Display Name (JA) | Data Type & Length | Required | Input Control | Validation |
|-------|-------------------|-------------------|-------------------|:--------:|---------------|------------|
| `code` | Code | クーポンコード | VARCHAR(50) | Yes | Input (text) | `@IsString()`, `@IsNotEmpty()`, `@MaxLength(50)`, `@Matches(/^[A-Za-z0-9_-]+$/)` |
| `description` | Description | 説明 | VARCHAR(500) | Yes | Textarea | `@IsString()`, `@IsNotEmpty()`, `@MaxLength(500)` |
| `discountType` | Discount Type | 割引種別 | VARCHAR(20) | Yes | Select | `@IsIn(['percentage', 'fixed'])` |
| `discountValue` | Discount Value | 割引額 | DECIMAL(10,2) | Yes | Input (number) | `@IsNumber()`, `@Min(0.01)` |
| `minOrderAmount` | Minimum Order Amount | 最低注文金額 | DECIMAL(10,2) | No | Input (number) | `@IsOptional()`, `@IsNumber()`, `@Min(0)` |
| `maxUses` | Maximum Uses | 最大使用回数 | INTEGER | No | Input (number) | `@IsOptional()`, `@IsInt()`, `@Min(1)` |
| `startsAt` | Start Date | 開始日時 | TIMESTAMPTZ | Yes | Input (datetime) | `@IsDate()`, `@IsNotEmpty()`; UTC |
| `expiresAt` | Expiry Date | 終了日時 | TIMESTAMPTZ | Yes | Input (datetime) | `@IsDate()`, `@IsNotEmpty()`, after `startsAt`; UTC |
| `isActive` | Active | 有効フラグ | BOOLEAN | Yes | Switch | `@IsBoolean()`, default true |

### 7.2 Input Specification — Update Promotion (入力定義)

Same as Create Promotion, with all fields optional (partial update). `code` field is read-only on update.

### 7.3 Input Specification — Validate Coupon (入力定義)

| Field | Display Name (EN) | Display Name (JA) | Data Type & Length | Required | Input Control | Validation |
|-------|-------------------|-------------------|-------------------|:--------:|---------------|------------|
| `code` | Coupon Code | クーポンコード | VARCHAR(50) | Yes | Input (text) | `@IsString()`, `@IsNotEmpty()` |
| `orderAmount` | Order Amount | 注文金額 | DECIMAL(10,2) | Yes | Input (number) | `@IsNumber()`, `@Min(0)` |

### 7.4 Output Specification — Promotion Detail (出力定義)

| Field | Data Source | Display Format |
|-------|-------------|----------------|
| `id` | `promotions.id` | UUID string |
| `merchantId` | `promotions.merchant_id` | UUID string |
| `code` | `promotions.code` | String |
| `description` | `promotions.description` | String |
| `discountType` | `promotions.discount_type` | 'percentage' / 'fixed' |
| `discountValue` | `promotions.discount_value` | Decimal (MMK, serialized as string) |
| `minOrderAmount` | `promotions.min_order_amount` | Decimal or null (MMK, serialized as string) |
| `maxUses` | `promotions.max_uses` | Integer or null |
| `usedCount` | `promotions.used_count` | Integer |
| `startsAt` | `promotions.starts_at` | ISO 8601 UTC timestamp |
| `expiresAt` | `promotions.expires_at` | ISO 8601 UTC timestamp |
| `isActive` | `promotions.is_active` | Boolean |
| `createdAt` | `promotions.created_at` | ISO 8601 UTC timestamp |

### 7.5 Output Specification — Coupon Validation Result (出力定義)

| Field | Data Source | Display Format |
|-------|-------------|----------------|
| `valid` | Validation result | Boolean |
| `discountType` | `promotions.discount_type` | 'percentage' / 'fixed' |
| `discountValue` | `promotions.discount_value` | Decimal (MMK, serialized as string) |
| `discountAmount` | Calculated | Decimal (MMK — discount applied to order, serialized as string) |
| `finalAmount` | Calculated | Decimal (MMK — order total after discount, >= 0, serialized as string) |
| `errorCode` | Validation failure | String (only on invalid) |
| `message` | Validation failure | String (only on invalid) |

---

## 8. Input Validation Rules

### 8.1 Promotion Creation Validation (Strict Mode)

| Field | Validation Rule | Error Message (EN) | Error Message (JA) |
|-------|-----------------|--------------------|--------------------|
| `code` | Required, 1-50 chars, alphanumeric, hyphens, underscores only | "Code is required" / "Code must not exceed 50 characters" / "Code can only contain letters, numbers, hyphens, and underscores" | "コードは必須です" / "コードは50文字以内で入力してください" / "コードは英数字、ハイフン、アンダースコアのみ使用できます" |
| `description` | Required, 1-500 chars | "Description is required" / "Description must not exceed 500 characters" | "説明は必須です" / "説明は500文字以内で入力してください" |
| `discountType` | Required, must be 'percentage' or 'fixed' | "Discount type is required" / "Discount type must be 'percentage' or 'fixed'" | "割引種別は必須です" / "割引種別は'percentage'または'fixed'を選択してください" |
| `discountValue` | Required, > 0. Percentage: ≤ 100 | "Discount value is required" / "Discount value must be greater than 0" / "Percentage must be between 1 and 100" | "割引額は必須です" / "割引額は0より大きい値を入力してください" / "パーセンテージは1〜100の範囲で入力してください" |
| `minOrderAmount` | Optional, ≥ 0 | "Minimum order amount must be 0 or greater" | "最低注文額は0以上の値を入力してください" |
| `maxUses` | Optional, ≥ 1 | "Maximum uses must be 1 or greater" | "最大使用回数は1以上で入力してください" |
| `startsAt` | Required, valid UTC datetime | "Start date is required" | "開始日は必須です" |
| `expiresAt` | Required, must be after startsAt | "Expiry date is required" / "Expiry date must be after start date" | "終了日は必須です" / "終了日は開始日より後の日付を選択してください" |
| `isActive` | Optional, default true | — | — |

### 8.2 Coupon Validation Validation (Strict Mode)

| Field | Validation Rule | Error Message (EN) | Error Message (JA) |
|-------|-----------------|--------------------|--------------------|
| `code` | Required, non-empty | "Coupon code is required" | "クーポンコードは必須です" |
| `orderAmount` | Required, ≥ 0 | "Order amount is required" / "Order amount must be 0 or greater" | "注文金額は必須です" / "注文金額は0以上の値を入力してください" |

### 8.3 Validation Enforcement Layers

1. **Frontend (Client)**: React Hook Form + Zod schema validation with real-time feedback.
2. **Backend (Server)**: NestJS ValidationPipe + class-validator DTOs on all endpoints.
3. **Database (PostgreSQL)**: CHECK constraints and UNIQUE constraints as final guard.

---

## 9. Error Handling Specification

### 9.1 Error Response Structure

```json
{
  "statusCode": 400,
  "message": ["Code already exists", "Discount value must be greater than 0"],
  "error": "Bad Request",
  "timestamp": "2026-08-19T12:00:00.000Z",
  "path": "/api/v1/promotions"
}
```

### 9.2 Error Classification Table — Promotion CRUD

| HTTP Status | Error Code | Scenario | User-Facing Behavior |
|-------------|------------|----------|---------------------|
| `400` | `BAD_REQUEST` | Validation failures | Field-level inline errors + top banner |
| `400` | `PROMO_CODE_DUPLICATE` | Code already exists (global uniqueness) | "A promotion with this code already exists" |
| `400` | `PROMO_INVALID_DISCOUNT` | discountValue <= 0 | "Discount value must be greater than 0" |
| `400` | `PROMO_INVALID_TYPE` | Invalid discountType | "Discount type must be 'percentage' or 'fixed'" |
| `400` | `PROMO_INVALID_DATE_RANGE` | expiresAt <= startsAt | "Expiry date must be after start date" |
| `400` | `PROMO_PERCENTAGE_OUT_OF_RANGE` | discountValue > 100 for percentage | "Percentage must be between 1 and 100" |
| `401` | `UNAUTHORIZED` | Missing or invalid JWT | Redirect to login |
| `403` | `FORBIDDEN` | Not merchant/admin or not promotion owner | "You don't have permission to manage this promotion" |
| `403` | `MERCHANT_NOT_APPROVED` | Merchant license status is `pending` — promotion mutations restricted | Error toast/banner: "Your account is pending approval. Promotion management is not available at this time."; remain on the restricted list |
| `403` | `MERCHANT_REJECTED` | Merchant license status is `rejected` — promotion mutations restricted | Error toast/banner includes the rejection reason; remain on the restricted list |
| `404` | `PROMO_NOT_FOUND` | Promotion not found | "Promotion not found" with back link |
| `409` | `PROMO_EDIT_RESTRICTED` | Promotion has been used (usedCount > 0) and an edit was requested | "Cannot edit promotion that has already been used" |
| `409` | `PROMO_DELETE_RESTRICTED` | Promotion has been used (usedCount > 0) and a delete was requested | "Cannot delete promotion that has already been used" |
| `429` | `TOO_MANY_REQUESTS` | Rate limit exceeded | "Too many requests. Please wait {seconds} seconds" |
| `500` | `INTERNAL_SERVER_ERROR` | Server error | "Something went wrong. Please try again" |

### 9.3 Error Classification Table — Coupon Validation

| HTTP Status | Error Code | Scenario | User-Facing Behavior |
|-------------|------------|----------|---------------------|
| `400` | `PROMO_EXPIRED` | Current time > expires_at | "This coupon has expired" |
| `400` | `PROMO_MAX_USES_REACHED` | used_count >= max_uses | "This coupon has reached its usage limit" |
| `400` | `PROMO_MIN_ORDER_NOT_MET` | orderAmount < minOrderAmount | "Minimum order amount of {amount} MMK not met" |
| `400` | `PROMO_INACTIVE` | isActive = false | "This coupon is currently inactive" |
| `400` | `PROMO_NOT_FOUND` | Code does not exist | "Invalid coupon code" |
| `401` | `UNAUTHORIZED` | Missing or invalid JWT | Redirect to login |
| `500` | `INTERNAL_SERVER_ERROR` | Server error | "Something went wrong. Please try again" |

### 9.4 Frontend Error Display Behavior

- **Field-Level Validation**: Red border and inline text below invalid input.
- **Form-Level Summary**: Alert banner at top of form listing all errors.
- **Toast Notifications**: Used for API errors and successful actions.
- **Loading States**: Spinner on submit buttons during API calls.

---

## 10. Permission and Access Control

### 10.1 Authentication Requirements

- JSON Web Token (JWT) Bearer Token passed via `Authorization` header for merchant operations.

### 10.2 Protected Endpoints

| Endpoint | Access Level | Description |
|----------|-------------|-------------|
| `POST /api/v1/promotions` | Protected (Merchant/Admin) | Create new promotion |
| `GET /api/v1/promotions` | Protected (Merchant/Admin) | List merchant promotions |
| `PATCH /api/v1/promotions/:id` | Protected (Merchant/Admin) | Update promotion |
| `DELETE /api/v1/promotions/:id` | Protected (Merchant/Admin) | Delete promotion |
| `POST /api/v1/promotions/validate` | Protected (Buyer) | Validate coupon code at checkout |

### 10.3 Role-Based Access

| Role | Create Promotions | List Promotions | Edit Promotions | Delete Promotions | Validate Coupons |
|------|:-----------------:|:---------------:|:---------------:|:-----------------:|:----------------:|
| `merchant` | ✓ (own shop, approved license status) | ✓ (own promotions, approved license status) | ✓ (own promotions, approved license status, used_count = 0) | ✓ (own promotions, approved license status, used_count = 0) | ✗ |
| `buyer` | ✗ | ✗ | ✗ | ✗ | ✓ |
| `admin` | ✓ (all) | ✓ (all) | ✓ (all) | ✓ (all) | ✓ |

### 10.4 License Status Enforcement

Merchants with `pending` license status are restricted from all promotion management operations. The following enforcement applies:

**Backend (API Guard):**
```typescript
// Backend license status check — applied to all promotion CRUD endpoints
const merchant = await this.prisma.merchant.findUnique({ where: { userId: user.id } });
if (!merchant) throw new ForbiddenException('Merchant profile not found');
if (merchant.licenseStatus === 'pending') {
  throw new ForbiddenException({
    statusCode: 403,
    error: 'MERCHANT_NOT_APPROVED',
    message: 'Your account is pending approval. Promotion management is not available at this time.'
  });
}
```

**Frontend (Route Guard):**
```typescript
// Frontend route guard — applied to /merchant/promotions/* routes
const merchantProfile = await fetchMerchantProfile(user.id);
if (merchantProfile.licenseStatus === 'pending') {
  toast.error('Your account is pending approval. Promotion management is not available at this time.');
  router.push('/');
  return;
}
```

### 10.5 Ownership Enforcement

```typescript
// Backend ownership check example
const promotion = await this.prisma.promotion.findUnique({ where: { id } });
if (!promotion) throw new NotFoundException('Promotion not found');
if (user.role === 'merchant' && promotion.merchantId !== user.merchantId) {
  throw new ForbiddenException('You can only manage your own promotions');
}

// Usage check for edit/delete
if (promotion.usedCount > 0) {
  throw new ConflictException({
    statusCode: 409,
    error: 'PROMO_EDIT_RESTRICTED',
    message: 'Cannot edit/delete promotion that has already been used'
  });
}
```

### 10.6 Security Audit Logging

| Event | Data Logged | Retention |
|-------|-------------|-----------|
| `PROMOTION_CREATED` | merchantId, promotionId, code, timestamp | 90 days |
| `PROMOTION_UPDATED` | merchantId, promotionId, changes, timestamp | 90 days |
| `PROMOTION_DELETED` | merchantId, promotionId, timestamp | 90 days |
| `COUPON_VALIDATED` | buyerId, promotionId, code, orderAmount, isValid, timestamp | 30 days |
| `COUPON_APPLIED` | buyerId, promotionId, code, discountAmount, orderId, timestamp | 90 days |

---

## 11. Real-Time Notification Behavior

### 11.1 Promotion Status Notifications

| Event | Trigger | Action |
|-------|---------|--------|
| `PROMOTION_CREATED` | New promotion created | Display success toast on merchant dashboard |
| `PROMOTION_USAGE_MILESTONE` | used_count reaches 50%, 80%, 100% of max_uses | Display warning badge on promotion list |
| `PROMOTION_EXPIRED` | expires_at < now | Display expired badge on promotion list |
| `PROMOTION_DEACTIVATED` | Merchant toggles is_active to false | Promotion hidden from buyer coupon selection |

### 11.2 Low Usage Warning

| Event | Trigger | Action |
|-------|---------|--------|
| `LOW_USAGE_WARNING` | Promotion near expiration with low usage | Display warning: "Promotion {code} expires soon with only {used_count} uses" |

---

## 12. Screen Transition Specification

### 12.1 Inbound Navigation

| Source | Target | Condition |
|--------|--------|-----------|
| Merchant dashboard | `/merchant/promotions` | Click "Promotions" menu |
| Merchant promotion list | `/merchant/promotions/new` | Click "Add New Promotion" |
| Merchant promotion list | `/merchant/promotions/:id/edit` | Click "Edit" button |

### 12.2 Internal Navigation

| Source | Target | Trigger |
|--------|--------|---------|
| `/merchant/promotions` | `/merchant/promotions/new` | Click "Add New" button |
| `/merchant/promotions` | `/merchant/promotions/:id/edit` | Click "Edit" button |
| `/merchant/promotions/new` | `/merchant/promotions` | Click "Cancel" or save success |
| `/merchant/promotions/:id/edit` | `/merchant/promotions` | Click "Cancel" or save success |

### 12.3 Outbound Navigation

| Source | Target | Condition |
|--------|--------|-----------|
| `/merchant/promotions/new` | `/merchant/promotions` | Promotion created successfully |
| `/merchant/promotions/:id/edit` | `/merchant/promotions` | Promotion updated successfully |
| Checkout | `/api/v1/promotions/validate` | Buyer enters coupon code |

### 12.4 Error Navigation

| Source | Target | Condition |
|--------|--------|-----------|
| Any merchant promotion page | `/login` | 401 Unauthorized |
| Any merchant promotion page | `/unauthorized` | 403 Forbidden |
| Any merchant promotion page | `/merchant/promotions` | 403 `MERCHANT_NOT_APPROVED` or `MERCHANT_REJECTED` — remain on the restricted list and display the matching status banner |

---

## 13. Non-Functional Considerations

### 13.1 Performance Requirements

| Metric | Target |
|--------|--------|
| Promotion List API | ≤ 200 milliseconds |
| Coupon Validation API | ≤ 100 milliseconds |
| Promotion Create API | ≤ 500 milliseconds |

### 13.2 Caching Strategy

**Redis Cache Keys:**

- **`promo:detail:{code}`** — Promotion detail cache
  - TTL: Set to the exact remaining time until `expiresAt` using `EXPIRE`
  - Invalidation: On promotion update/delete, or after committed usage reaches `maxUses`

- **`promo:list:{merchantId}`** — Merchant promotion list cache
  - TTL: 2 minutes (120 seconds)
  - Invalidation: On any promotion CRUD operation for that merchant

- **`promo:quota:{code}`** — Optional reservation counter for limited-use promotions
  - TTL: Set to the exact remaining time until `expiresAt`
  - This counter is not authoritative; `promotions.used_count` is the source of truth
  - Never increment usage during coupon validation; commit usage atomically with order creation

**Cache Invalidation Triggers:**

- Promotion created → invalidate merchant list cache
- Promotion updated → invalidate detail cache + list cache
- Promotion deleted → invalidate detail cache + list cache + quota key
- Coupon validated → optional short-lived read cache only; do not mutate usage
- Coupon applied in a committed order → increment `used_count` atomically and invalidate detail/list caches
- Quota exhausted → invalidate detail cache; do not rewrite database status because no status column exists

### 13.3 Rate Limiting

| Endpoint | Rate Limit | Window |
|----------|-----------|--------|
| `POST /api/v1/promotions/validate` | 30 requests | 60 seconds per user |
| `POST /api/v1/promotions` | 10 requests | 60 seconds per merchant |

### 13.4 Responsive Design Requirements

| Breakpoint | Layout |
|------------|--------|
| Desktop (≥ 1024px) | Full-width promotion form, sidebar navigation |
| Tablet (768px – 1023px) | Full-width promotion form, collapsible sidebar |
| Mobile (< 768px) | Stacked form layout, bottom navigation |

### 13.5 Accessibility Requirements

| Requirement | Implementation |
|-------------|---------------|
| WCAG 2.1 AA | Semantic HTML, ARIA labels on all interactive elements |
| Keyboard Navigation | Tab order through form fields, buttons, switches |
| Screen Reader | Alt text for status badges, ARIA labels for actions |
| Color Contrast | Minimum 4.5:1 for text, 3:1 for UI components |
| Focus Indicators | Visible focus ring on all interactive elements |

---

## 14. Configurable Items (External Definitions)

Defined via `.env` configuration:

| Definition Key | Default Value | Description |
|----------------|---------------|-------------|
| `PROMO_CODE_MAX_LENGTH` | `50` | Maximum promotion code length |
| `PROMO_MIN_DISCOUNT_VALUE` | `0.01` | Minimum discount value in MMK |
| `PROMO_MAX_PERCENTAGE` | `100` | Maximum percentage discount value |
| `PROMO_CACHE_TTL_SECONDS` | `300` | Promotion detail cache TTL (5 min) |
| `PROMO_LIST_CACHE_TTL_SECONDS` | `120` | Promotion list cache TTL (2 min) |
| `PROMO_VALIDATE_RATE_LIMIT` | `30` | Max validation requests per window |
| `PROMO_VALIDATE_RATE_WINDOW` | `60` | Rate limit window in seconds |

---

## 15. Cross-Reference Traceability Matrix

### 15.1 Requirements Definition Traceability

| Requirement ID | Requirement Description | Covered By (This Document) |
|----------------|-------------------------|----------------------------|
| M-PROMO-001 | Merchant can create, edit, delete promotions | UC-PROMO-001, UC-PROMO-003, UC-PROMO-004, Sec 6.1, 6.2, 6.3 |
| M-PROMO-002 | Promotion supports percentage and fixed discount types | BR-PROMO-003, BR-PROMO-004, Sec 7.1 |
| M-PROMO-003 | Promotion can set minimum order amount | BR-PROMO-017, Sec 7.1 |
| M-PROMO-004 | Promotion can set maximum usage count and expiration date | BR-PROMO-015, BR-PROMO-016, Sec 7.1 |
| M-PROMO-005 | Promotion usage statistics are displayed | UC-PROMO-006, Sec 5.1 |
| M-PROMO-006 | Promotion can be edited and deleted by merchant | UC-PROMO-003, UC-PROMO-004, Sec 6.2, 6.3 |

### 15.2 Database Design Traceability

| Database Table | Relevant Functional Operations |
|----------------|-------------------------------|
| `promotions` | Create (INSERT), List (SELECT), Update (PATCH), Delete (DELETE), Validate (SELECT) |

### 15.3 Related Document References

| Document ID | Document Name | File Path |
|-------------|---------------|-----------|
| SKM-REQ-001 | Requirements Definition | `docs/core-work/要件定義書_REQUIREMENT_SPEC.md` |
| SKM-DBS-001 | Database Design Specification | `docs/core-work/データベース設計書_DATABASE_SPEC.md` |
| SKM-DEV-001 | Development Rules | `docs/core-work/開発ルール_DEVELOPMENT_RULES.md` |
| SKM-FDS-PROD-001 | Product Management Spec | `docs/screen/Product_Management/商品管理画面_機能設計書.md` |

---

## 16. Redis Cache Invalidation Strategy

> **Note** — No new PostgreSQL database tables are added. This section covers Redis Cache Layer only.

### 16.1 Time-Based Expiry (TTL)

**Redis `EXPIRE` command** is used to set TTL with **exact promo expiration timestamps**.

**TTL Calculation Logic:**

- **Step 1: Get expiration time** — Retrieve `promo.expires_at` from database as UTC timestamp.
- **Step 2: Calculate remaining TTL** — `ttlSeconds = Math.max(0, Math.floor((promo.expires_at - Date.now()) / 1000))`. If `ttlSeconds = 0`, delete key immediately.
- **Step 3: Set Redis key with TTL** — `SET promo:detail:{code} {json_data} EX {ttlSeconds}`

**TTL Application Examples:**

- Promotion created at 10:00, expires at 18:00 (8 hours) → TTL = 28800 seconds
- Promotion created at 10:00, expires at 10:30 (30 minutes) → TTL = 1800 seconds
- Promotion already expired → TTL = 0 → Key deleted immediately

**Backend Implementation:**

```typescript
// Backend — TTL-based cache setting
async setPromotionCache(promo: Promotion): Promise<void> {
  const now = new Date();
  const expiresAt = new Date(promo.expires_at);
  const ttlSeconds = Math.max(0, Math.floor((expiresAt.getTime() - now.getTime()) / 1000));

  if (ttlSeconds === 0) {
    await this.redis.del(`promo:detail:${promo.code}`);
    return;
  }

  const cacheData = JSON.stringify(promo);
  await this.redis.set(`promo:detail:${promo.code}`, cacheData, 'EX', ttlSeconds);
}
```

### 16.2 Atomic Counters for Quotas

**Redis Atomic Commands** may reserve capacity for **limited-use promotions** without race conditions. The database `used_count` remains authoritative, and reservation logic is separate from coupon validation.

**Quota Key Structure:**

- **Key Pattern:** `promo:quota:{code}`
- **Initial Value:** `max_uses` (integer)
- **TTL:** Same as promotion expiration time

**Atomic Decrement Strategy:**

- **Step 1: Check quota availability** — `GET promo:quota:{code}` → returns remaining count. If `null` or `0` → quota exhausted, reject coupon.
- **Step 2: Atomic decrement** — Use **Lua script** for atomic check-and-decrement.

**Lua Script for Optional Reservation:**

```lua
-- Redis Lua Script — Atomic coupon use with quota check
local key = KEYS[1]  -- promo:quota:{code}
local max_uses = tonumber(ARGV[1])

-- Get current quota
local current = tonumber(redis.call('GET', key))

-- If key doesn't exist, initialize with max_uses
if current == nil then
  redis.call('SET', key, max_uses)
  current = max_uses
end

-- Check if quota available
if current <= 0 then
  return -1  -- Quota exhausted
end

-- Atomic decrement
local new_value = redis.call('DECR', key)

-- If went below 0, restore and reject
if new_value < 0 then
  redis.call('INCR', key)
  return -1  -- Quota exhausted
end

return new_value  -- Returns remaining count
```

**Backend Integration:**

```typescript
// Backend — Atomic quota check
async useCouponQuota(code: string, maxUses: number): Promise<boolean> {
  const luaScript = `
    local key = KEYS[1]
    local max_uses = tonumber(ARGV[1])
    local current = tonumber(redis.call('GET', key))
    if current == nil then
      redis.call('SET', key, max_uses)
      current = max_uses
    end
    if current <= 0 then return -1 end
    local new_value = redis.call('DECR', key)
    if new_value < 0 then
      redis.call('INCR', key)
      return -1
    end
    return new_value
  `;

  const result = await this.redis.eval(
    luaScript,
    1,  // number of keys
    `promo:quota:${code}`,
    maxUses.toString()
  );

  return result !== -1;
}
```

**Quota Exhaustion Behavior:**

- Quota hits zero → Redis key `promo:quota:{code}` value = `0`
- Next coupon attempt → Lua script returns `-1` → Reject
- Auto-invalidation → Detail cache `promo:detail:{code}` also invalidated
- Status update → Promotion status set to `MAX_USES_REACHED` in cache

### 16.3 Event-Driven Invalidation

**Event-Driven Invalidation** — When promotion changes occur, **immediately** invalidate cache.

**Event-Driven Invalidation Triggers:**

- **Merchant deletes promotion**
  - Event: `PROMOTION_DELETED`
  - Action: `DEL promo:detail:{code}` + `DEL promo:quota:{code}`
  - Service: `PromotionsService.deletePromotion()`

- **Merchant updates promotion**
  - Event: `PROMOTION_UPDATED`
  - Action: `DEL promo:detail:{code}` (re-fetch on next read)
  - Service: `PromotionsService.updatePromotion()`

- **Quota exhausted**
  - Event: `QUOTA_EXHAUSTED`
  - Action: `DEL promo:detail:{code}` + status update
  - Service: `PromotionQuotaService.decrementQuota()`

### 16.4 Cache Invalidation Flow

**Complete Cache Lifecycle:**

- **Create Promotion**
  - DB: INSERT promotion
  - Redis: `SET promo:detail:{code} {data} EX {ttl}`
  - Redis: `SET promo:quota:{code} {max_uses} EX {ttl}`

- **Update Promotion**
  - DB: UPDATE promotion
  - Redis: `DEL promo:detail:{code}` (invalidate cache)
  - Next read: Re-fetch from DB, repopulate cache

- **Delete Promotion (Hard Delete)**
  - DB: DELETE promotion
  - Redis: `DEL promo:detail:{code}` + `DEL promo:quota:{code}`
  - Next read: Returns null (not found)

- **Use Coupon (Quota Decrement)**
  - Redis Lua: `DECR promo:quota:{code}`
  - If quota = 0: `DEL promo:detail:{code}` + `DEL promo:quota:{code}`
  - Next read: Returns null or status `MAX_USES_REACHED`

- **Promotion Expires (TTL Auto-Delete)**
  - Redis: Key auto-deleted when TTL reaches 0
  - No application code needed — Redis handles automatically

### 16.5 Redis Cache Patterns Summary

- **`promo:detail:{code}`** — Single promotion detail cache
  - Set with exact TTL (expires_at - now)
  - Invalidated on: update, delete, quota exhaustion

- **`promo:list:{merchantId}`** — Merchant promotion list cache
  - TTL: 120 seconds (short-lived)
  - Invalidated on: any CRUD operation

- **`promo:quota:{code}`** — Atomic quota counter
  - Set with exact TTL (expires_at - now)
  - Used with Lua script for atomic decrement
  - Auto-invalidated when counter reaches zero

**Redis Performance Targets:**

- **Cache Hit Rate** — >= 95%
- **Cache Read Latency** — <= 1ms (Redis is in-memory)
- **Cache Write Latency** — <= 2ms
- **Lua Script Execution** — <= 5ms

---

*End of Functional Specification (Promotion Management)*
