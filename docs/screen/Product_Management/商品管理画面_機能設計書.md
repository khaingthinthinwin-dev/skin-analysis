# Functional Specification (機能設計書) — Product Management

---

## Document Control

| Attribute | Value |
|-----------|-------|
| **Document ID** | SKM-FDS-PROD-001 |
| **Target Screen** | Product Management (商品管理) |
| **Subsystem** | Product — Merchant CRUD Operations, Inventory Management |
| **Function ID** | FN-PROD-001 |
| **Version** | 1.5 |
| **Created** | 2026-08-05 |
| **Last Updated** | 2026-08-20 |
| **Author** | Software Architect |
| **Status** | Released (承認済み) |
| **Classification** | Internal — Engineering Division |

---

## Document Revision History

| Version | Date | Author | Description of Changes |
|---------|------|--------|------------------------|
| 1.0 | 2026-08-05 | Software Architect | Initial functional specification for Product Management covering merchant product CRUD, inventory management, and related business rules. |
| 1.1 | 2026-08-07 | Software Architect | Removed buyer-related information. Document now focuses exclusively on merchant product management (CRUD, inventory, image management). |
| 1.2 | 2026-08-12 | Software Architect | Added active order deletion guard (BR-PROD-024, BR-PROD-025). Products with orders in pending/confirmed/processing/delivered status cannot be soft-deleted. Updated UC-PROD-003, Section 6.3, Section 9.2, and Section 10.4. |
| 1.3 | 2026-08-14 | Software Architect | Aligned with REQUIREMENT_SPEC v1.5 and DATABASE_SPEC v2.0: renamed `accountStatus` to `licenseStatus` for database consistency, updated error code from `MERCHANT_ACCOUNT_PENDING` to `MERCHANT_NOT_APPROVED`, fixed order status for deletion guard (`done` → `delivered`). |
| 1.5 | 2026-08-20 | Software Architect | Added Section 16: Implementation Status & Gap Analysis. Documented current codebase state (empty stubs in `catalog/products/`, orphaned DTOs, missing guard, missing tests) and added implementation traceability to each use case. Bumped version to 1.5 (skipped 1.4 which was never released). |

---

## Table of Contents

1. [Functional Overview](#1-functional-overview)
2. [Use Cases and Business Workflow](#2-use-cases-and-business-workflow)
3. [State Transition Specification](#3-state-transition-specification)
4. [Business Rules](#4-business-rules)
5. [Screen Specifications](#5-screen-specifications)
6. [Functional Operation Specification](#6-functional-operation-specification)
7. [Input / Output Specification](#7-input--output-specification)
8. [Input Validation Rules](#8-input-validation-rules)
9. [Error Handling Specification](#9-error-handling-specification)
10. [Permission and Access Control](#10-permission-and-access-control)
11. [Real-Time Notification Behavior](#11-real-time-notification-behavior)
12. [Screen Transition Specification](#12-screen-transition-specification)
13. [Non-Functional Considerations](#13-non-functional-considerations)
14. [Configurable Items (External Definitions)](#14-configurable-items-external-definitions)
15. [Cross-Reference Traceability Matrix](#15-cross-reference-traceability-matrix)
16. [Implementation Status & Gap Analysis](#16-implementation-status--gap-analysis)

---

## 1. Functional Overview

### 1.1 Purpose and Scope

This subsystem manages the complete lifecycle of skincare products within the Cosmetics Finder marketplace. It provides merchants with full CRUD capabilities for product listings, inventory management, and image uploads.

The Product Management subsystem is central to the marketplace platform, enabling merchants to efficiently manage their product catalog, inventory, and product information.

### 1.2 Functional Responsibilities

This subsystem is responsible for the following core functional areas:

1. **Product Creation** — Merchants can create new product listings with name, description, price, category, images, skin type compatibility, ingredients, and tags.
2. **Product Editing** — Merchants can update existing product details, images, pricing, and metadata.
3. **Product Deletion** — Merchants can soft-delete products (set `is_active = false`), hiding them from searches while retaining data. **Note: Products with active orders (pending/confirmed/processing/delivered) cannot be deleted.**
4. **Image Management** — Merchants can upload, reorder, and remove product images (max 10 images, 5MB each, JPG/PNG/WebP).
5. **Inventory Management** — Merchants can view and update stock quantities, set low stock thresholds, and receive low stock warnings.
6. **Featured Products** — Merchants can flag products as featured; featured products appear on the home page.
7. **Active/Inactive Toggle** — Merchants can toggle product visibility without deleting.
8. **License Status Guard** — Merchants with `pending` license status are restricted from all product CRUD operations. Access to any product management page triggers an error toast and automatic redirect to the home page.

### 1.3 Target Users

| Attribute | Value |
|-----------|-------|
| **Primary Actor (Merchant)** | Authenticated merchant managing their product catalog (license status must be `approved`) |
| **Primary Actor (Admin)** | Admin with full product management access |
| **Restricted Actor (Pending Merchant)** | Authenticated merchant with `pending` license status — all product CRUD operations are restricted |
| **Required Authentication** | JWT Bearer Token for merchant operations |
| **Data Scope** | Merchant: own products only. Admin: all products. Pending: no access. |

### 1.4 Relationships with Other Functions and Peripheral Systems

```text
┌──────────────────────┐      ┌─────────────────────────────────────┐
│   Merchant Actor     │      │     products / categories           │
│   (Manages Products) ├─────►│  CRUD operations, image upload      │
└──────────────────────┘      └──────────────┬────────────────────┘
                                             │ Reads/Writes
                                             ▼
                                  ┌────────────────────────┐
                                  │  Products Module       │
                                  └──────────┬─────────────┘
                                             │
                        ┌────────────────────┼────────────────────┐
                        ▼                    ▼                    ▼
             ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
             │  Categories      │  │  Reviews          │  │  Order Items     │
             │  (Tree Structure)│  │  (Ratings)        │  │  (Sales History) │
             └──────────────────┘  └──────────────────┘  └──────────────────┘
                                             │
                                  ┌─────────────────────────────────────┐
                                  │     Redis Cache                     │
                                  │  Product cache, list cache,         │
                                  │  category cache                      │
                                  └─────────────────────────────────────┘
```

### 1.5 Inputs / Outputs

| Input Information | Data Category | Source / Description |
|-------------------|---------------|----------------------|
| `name` | User Input | Product display name |
| `slug` | System Generated | URL-friendly identifier (auto-generated from name) |
| `description` | User Input | Detailed product description (rich text) |
| `shortDescription` | User Input | Brief product summary |
| `price` | User Input | Product selling price |
| `compareAtPrice` | User Input | Original price for discount display |
| `sku` | User Input | Stock Keeping Unit identifier |
| `categoryId` | User Input | Product category selection |
| `images` | File Upload | Product images (JPG, PNG, WebP) |
| `skinTypes` | User Input | Compatible skin types multi-select |
| `ingredients` | User Input | Product ingredients list |
| `tags` | User Input | Product tags for search/filter |
| `stockQuantity` | User Input | Current inventory count |
| `lowStockThreshold` | User Input | Low stock warning threshold |
| `isActive` | User Input | Product visibility toggle |
| `isFeatured` | User Input | Featured product flag |

| Output Information | Data Category | Destination / Description |
|--------------------|---------------|---------------------------|
| `product` | Product DTO | Full product data with relations |
| `categories` | Category[] DTO | Category tree structure |

### 1.6 Related Documents

| No. | Document ID | Document Name | File Path / Reference | Remarks |
|-----|-------------|---------------|----------------------|---------|
| 1 | SKM-REQ-001 | Requirements Definition | `docs/core-work/要件定義書_REQUIREMENT_SPEC.md` | M-PROD-001~010 |
| 2 | SKM-DBS-001 | Database Design Specification | `docs/core-work/データベース設計書_DATABASE_SPEC.md` | `products`, `categories`, `reviews`, `wishlists` tables |
| 3 | SKM-DEV-001 | Development Rules | `docs/core-work/開発ルール_DEVELOPMENT_RULES.md` | Naming conventions, RBAC, API standards |

---

## 2. Use Cases and Business Workflow

### 2.1 Use Case Catalog

| UC-ID | Use Case Name | Precondition | Postcondition | Triggering Actor |
|-------|---------------|--------------|---------------|------------------|
| UC-PROD-001 | Create New Product | Merchant is authenticated, has approved shop, and license status is `approved`. Merchants with `pending` status are restricted. | New product record created in `products` table. Product appears in merchant's product list. | Merchant |
| UC-PROD-002 | Edit Existing Product | Merchant is authenticated. Product belongs to merchant. License status is `approved`. Merchants with `pending` status are restricted. | Product record updated. Cache invalidated. | Merchant |
| UC-PROD-003 | Delete Product (Soft) | Merchant is authenticated. Product belongs to merchant. Product has no active orders (all orders must be in 'delivered' or 'cancelled' status). License status is `approved`. Merchants with `pending` status are restricted. | Product `is_active` set to false. Product hidden from searches. | Merchant |
| UC-PROD-004 | Upload Product Images | Merchant is authenticated. Product belongs to merchant. License status is `approved`. Merchants with `pending` status are restricted. | Images uploaded, URLs stored in product `images` array. | Merchant |
| UC-PROD-005 | Update Stock Quantity | Merchant is authenticated. Product belongs to merchant. License status is `approved`. Merchants with `pending` status are restricted. | Stock quantity updated. Low stock warning if below threshold. | Merchant |
| UC-PROD-006 | Toggle Product Active/Inactive | Merchant is authenticated. Product belongs to merchant. License status is `approved`. Merchants with `pending` status are restricted. | Product visibility toggled. | Merchant |
| UC-PROD-007 | Set Product as Featured | Merchant is authenticated. Product belongs to merchant. License status is `approved`. Merchants with `pending` status are restricted. | Product `is_featured` flag updated. | Merchant |
| UC-PROD-008 | View Merchant Product List | Merchant is authenticated. License status is `approved`. Merchants with `pending` status are restricted and redirected to home. | List of merchant's own products displayed. | Merchant |

### 2.2 Primary Business Workflow — Merchant Product Management

```
                    ┌──────────────────────┐
                    │  Merchant Logs In    │
                    │  (JWT Authenticated) │
                    └──────────┬───────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │  License Status      │
                    │  Check               │
                    └──────────┬───────────┘
                               │
                ┌──────────────┴──────────────┐
                ▼                             ▼
     ┌──────────────────┐          ┌──────────────────┐
     │ Status = pending │          │ Status = approved│
     │ → Show Error     │          │ → Proceed to     │
     │   Toast          │          │   Dashboard      │
     │ → Redirect to /  │          └────────┬─────────┘
     └──────────────────┘                   │
                                            ▼
                                 ┌──────────────────────┐
                                 │  Merchant Dashboard  │
                                 │  /merchant/products  │
                                 └──────────┬───────────┘
                                            │
                               ┌────────────────┼────────────────────┐
                               ▼                ▼                    ▼
                    ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
                    │ View Product    │ │ Add New Product │ │ Edit Product    │
                    │ List            │ │ (UC-PROD-001)   │ │ (UC-PROD-002)   │
                    └────────┬────────┘ └────────┬────────┘ └────────┬────────┘
                             │                   │                   │
                             ▼                   ▼                   ▼
                    ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
                    │ Filter/Search   │ │ Fill Product    │ │ Update Fields   │
                    │ Products        │ │ Form            │ │ & Images        │
                    └────────┬────────┘ └────────┬────────┘ └────────┬────────┘
                             │                   │                   │
                             │                   ▼                   ▼
                             │          ┌─────────────────┐ ┌─────────────────┐
                             │          │ Upload Images   │ │ Save Changes    │
                             │          │ (UC-PROD-004)   │ │ (UC-PROD-002)   │
                             │          └────────┬────────┘ └────────┬────────┘
                             │                   │                   │
                             │                   ▼                   ▼
                             │          ┌─────────────────┐ ┌─────────────────┐
                             │          │ Submit Product  │ │ Cache Invalidated│
                             │          │ (UC-PROD-001)   │ │ Product Updated  │
                             │          └────────┬────────┘ └─────────────────┘
                             │                   │
                             │                   ▼
                             │          ┌─────────────────┐
                             │          │ Product Created │
                             │          │ in Database     │
                             │          └─────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ Toggle Active/  │
                    │ Inactive        │
                    │ (UC-PROD-006)   │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ Delete Product  │
                    │ (Soft Delete)   │
                    │ (UC-PROD-003)   │
                    └─────────────────┘
```

### 2.3 Workflow Critical Path Summary

| Step | Action | Status Before | Status After | Assigned To |
|:----:|--------|---------------|--------------|-------------|
| 1 | Merchant navigates to /merchant/products | Authenticated | — | Merchant |
| 2 | System checks license status | — | Status verified | System |
| 3 | If status = pending → show error toast, redirect to `/` | — | Redirected | System |
| 4 | If status = approved → proceed to product list | — | List displayed | System |
| 5 | Merchant clicks "Add New Product" | — | Form Displayed | System |
| 6 | Merchant fills product form | — | — | Merchant |
| 7 | Merchant uploads images | — | Images Attached | Merchant |
| 8 | Merchant submits product | — | Product Created | System |
| 9 | Product appears in merchant list | — | — | System |

### 2.4 Relevant Requirements Covered

| Requirement ID | Requirement Summary |
|----------------|---------------------|
| M-PROD-001 | Merchant can create new products |
| M-PROD-002 | Merchant can edit existing products |
| M-PROD-003 | Merchant can delete products (soft delete) |
| M-PROD-004 | Merchant can upload product images |
| M-PROD-005 | Merchant can manage inventory (stock quantity) |
| M-PROD-006 | Merchant can view own product list |
| M-PROD-007 | Merchant can toggle product active/inactive |
| M-PROD-008 | Merchant can set product as featured |
| M-PROD-009 | Product creation requires: name, category, price, description |
| M-PROD-010 | Product images support JPG, PNG, WebP (max 5MB each) |

---

## 3. State Transition Specification

### 3.1 Product States

| State | Description | Can Edit | Can Order |
|-------|-------------|:--------:|:---------:|
| `DRAFT` | Product created but not yet active | ✓ | ✗ |
| `ACTIVE` | Product is live and available for purchase | ✓ | ✓ |
| `INACTIVE` | Product hidden by merchant (soft delete) | ✓ | ✗ |
| `OUT_OF_STOCK` | Product has zero stock | ✓ | ✗ |

### 3.2 Product Lifecycle Transitions

| Transition ID | Origin State | Target State | Trigger Action | Guard Conditions |
|---------------|--------------|--------------|----------------|------------------|
| TR-PROD-01 | — | `ACTIVE` | Create product (default) | Valid product data, merchant authenticated |
| TR-PROD-02 | `ACTIVE` | `INACTIVE` | Toggle active off | Product belongs to merchant |
| TR-PROD-03 | `INACTIVE` | `ACTIVE` | Toggle active on | Product belongs to merchant |
| TR-PROD-04 | `ACTIVE` | `OUT_OF_STOCK` | Stock decremented to 0 | Order creation |
| TR-PROD-05 | `OUT_OF_STOCK` | `ACTIVE` | Stock replenished | Merchant updates stock |
| TR-PROD-06 | `ACTIVE` | `INACTIVE` | Soft delete | Product belongs to merchant AND no active orders (all orders in 'done' status) |
| TR-PROD-07 | `INACTIVE` | `ACTIVE` | Restore product | Product belongs to merchant |

### 3.3 Image Upload States

| State | Description | Max Files |
|-------|-------------|:---------:|
| `EMPTY` | No images uploaded | 0 |
| `PARTIAL` | 1-9 images uploaded | 9 |
| `FULL` | 10 images uploaded (maximum) | 10 |

---

## 4. Business Rules

### 4.1 Product Creation Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROD-001 | Required Fields | Product must have: name, categoryId, price, description. | Backend (DTO validation) + Frontend (Zod schema) |
| BR-PROD-002 | Price Positive | Price must be greater than 0. | Backend (DB constraint `chk_products_price`) |
| BR-PROD-003 | Compare Price | compareAtPrice must be greater than price (if provided). | Backend (DTO validation) |
| BR-PROD-004 | SKU Uniqueness | SKU must be unique across all products (if provided). | Backend (DB constraint `uq_products_sku`) |
| BR-PROD-005 | Slug Generation | Slug auto-generated from product name, must be unique. | Backend (slug util) |
| BR-PROD-006 | Category Existence | Category must exist and be valid. | Backend (DB FK constraint) |
| BR-PROD-007 | Default Status | New products default to `is_active = true`. | Backend (DB default) |
| BR-PROD-008 | Default Featured | New products default to `is_featured = false`. | Backend (DB default) |

### 4.2 Product Image Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROD-009 | Max Images | Maximum 10 images per product. | Backend (DTO validation) |
| BR-PROD-010 | File Size | Maximum 5MB per image. | Backend (file upload validation) |
| BR-PROD-011 | File Types | Allowed: JPG, PNG, WebP. | Backend (MIME type validation) |
| BR-PROD-012 | Primary Image | First image is the primary/cover image. | Frontend (display logic) |
| BR-PROD-013 | File Naming | UUID-based filenames to prevent conflicts. | Backend (upload service) |

### 4.3 Product Status Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROD-014 | Active Visibility | Only `is_active = true` products are publicly visible. | Backend (query filter) |
| BR-PROD-015 | Inactive Hidden | Inactive products hidden from public view but visible to merchant. | Backend (role-based query) |
| BR-PROD-016 | Soft Delete | Delete sets `is_active = false`, does not remove record. | Backend (service logic) |
| BR-PROD-017 | Featured Flag | Featured products appear on home page. | Backend (query filter) |

### 4.4 Inventory Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROD-018 | Stock Non-Negative | Stock quantity cannot go below 0. | Backend (DB constraint `chk_products_stock`) |
| BR-PROD-019 | Low Stock Threshold | Default threshold is 10 units. Warning when stock ≤ threshold. | Backend (service logic) |
| BR-PROD-020 | Out of Stock | Products with 0 stock are marked as out of stock. | Backend (status logic) |
| BR-PROD-021 | Atomic Decrement | Stock decremented atomically on order creation. | Backend (DB transaction) |

### 4.5 Ownership Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROD-022 | Merchant Ownership | Merchants can only edit/delete their own products. | Backend (service check) |
| BR-PROD-023 | Admin Override | Admins can manage all products. | Backend (RBAC) |

### 4.6 Product Deletion Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROD-024 | Active Order Deletion Guard | Product cannot be soft-deleted if it has any associated orders with status NOT IN ('delivered', 'cancelled'). Only products with no orders or all orders in terminal states ('delivered' or 'cancelled') can be deleted. | Backend (service logic) |
| BR-PROD-025 | Deletion Restriction Message | When deletion is blocked due to active orders, display message: "Cannot delete product with active orders. All orders must be completed first." | Backend (error response) |

### 4.7 Merchant License Status Rules

| Rule ID | Rule Name | Description | Enforcement Layer |
|---------|-----------|-------------|-------------------|
| BR-PROD-026 | Pending License Restriction | Merchants with license status `pending` are restricted from all Product CRUD operations (Create, Read, Update, Delete, Stock Update). Any attempt to access product management pages (`/merchant/products/*`) must be blocked. | Backend (middleware/guard) + Frontend (route guard) |
| BR-PROD-027 | Pending License Redirect | When a merchant with `pending` status attempts to access `/merchant/products/*`, display an error toast message "Your account is pending approval. Product management is not available at this time." and automatically redirect to the home page (`/`). | Frontend (route guard + toast notification) |

---

## 5. Screen Specifications

### 5.1 Screen: Merchant Product List (`/merchant/products`)

**Purpose:** Allow merchants to view and manage their product catalog.

#### 5.1.1 UI Elements

**Action Bar:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-01 | Add Product Button | Button (primary) | `merchant.products.addNew` | Yes | Navigate to product creation form |
| EL-02 | Search Input | Input (text) | `merchant.products.search` | No | Search within own products |
| EL-03 | Status Filter | Select | `merchant.products.status` | No | Filter by active/inactive |
| EL-04 | Bulk Actions | Button Group | — | No | Bulk activate/deactivate/delete |

**Product Table:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-05 | Product Thumbnail | Image (small) | — | Yes | Primary product image |
| EL-06 | Product Name | Text | — | Yes | Product display name |
| EL-07 | SKU | Text | — | No | Stock Keeping Unit |
| EL-08 | Price | Text (currency) | — | Yes | Current price |
| EL-09 | Stock | Text (number) | — | Yes | Current stock quantity with low stock warning |
| EL-10 | Status Badge | Badge | — | Yes | Active/Inactive status |
| EL-11 | Featured Badge | Badge | — | No | Featured product indicator |
| EL-12 | Edit Button | Button (ghost) | — | Yes | Navigate to edit form |
| EL-13 | Delete Button | Button (ghost, danger) | — | Yes | Soft delete with confirmation |
| EL-14 | Toggle Active | Switch | — | Yes | Toggle product visibility |
| EL-15 | Toggle Featured | Switch | — | No | Toggle featured status |

**Default State:**
- Products sorted by newest first
- All products shown (active and inactive)
- Stock warning highlighted in red when below threshold

### 5.2 Screen: Product Form (`/merchant/products/new` and `/merchant/products/:id/edit`)

**Purpose:** Allow merchants to create or edit product listings.

#### 5.2.1 UI Elements

**Basic Information:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-16 | Product Name | Input (text) | `merchant.products.name` | Yes | Product display name |
| EL-17 | Short Description | Textarea | `merchant.products.shortDesc` | Yes | Brief summary (max 500 chars) |
| EL-18 | Description | Rich Text Editor | `merchant.products.description` | Yes | Detailed product description |
| EL-19 | Category | Select (tree) | `merchant.products.category` | Yes | Category selection with nested options |
| EL-20 | SKU | Input (text) | `merchant.products.sku` | No | Stock Keeping Unit |

**Pricing & Inventory:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-21 | Price | Input (number) | `merchant.products.price` | Yes | Selling price |
| EL-22 | Compare At Price | Input (number) | `merchant.products.comparePrice` | No | Original price for discount display |
| EL-23 | Stock Quantity | Input (number) | `merchant.products.stock` | Yes | Current inventory count |
| EL-24 | Low Stock Threshold | Input (number) | `merchant.products.lowStockThreshold` | No | Warning threshold (default: 10) |

**Product Images:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-25 | Image Upload | File Upload (drag & drop) | `merchant.products.images` | Yes | Upload product images |
| EL-26 | Image Preview | Image Grid | — | Yes | Preview uploaded images |
| EL-27 | Reorder Handles | Drag Handle | — | No | Reorder image priority |
| EL-28 | Delete Image | Button (icon) | — | No | Remove uploaded image |

**Product Attributes:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-29 | Skin Types | Checkbox Group | `merchant.products.skinTypes` | No | Compatible skin types |
| EL-30 | Ingredients | Tag Input | `merchant.products.ingredients` | No | Product ingredients |
| EL-31 | Tags | Tag Input | `merchant.products.tags` | No | Product tags for search |
| EL-32 | Is Active | Switch | `merchant.products.isActive` | Yes | Product visibility toggle |
| EL-33 | Is Featured | Switch | `merchant.products.isFeatured` | No | Featured product flag |

**Form Actions:**

| Element ID | Element Name | Element Type | i18n Key | Required | Description |
|------------|--------------|--------------|----------|:--------:|-------------|
| EL-34 | Save Button | Button (primary) | `common.save` | Yes | Submit product form |
| EL-35 | Cancel Button | Button (ghost) | `common.cancel` | No | Return to product list |
| EL-36 | Save Draft | Button (outline) | `merchant.products.saveDraft` | No | Save as inactive |

**Default State (Create):**
- Name input auto-focused
- Is Active switch ON by default
- Is Featured switch OFF by default
- Stock quantity defaults to 0
- Low stock threshold defaults to 10

**Default State (Edit):**
- All fields populated with existing product data
- Image preview shows current images
- Save button text changes to "Update"

---

## 6. Functional Operation Specification

### 6.1 Operation: Create Product (Merchant)

| Attribute | Specification |
|-----------|---------------|
| **Trigger** | Submit product form on `/merchant/products/new` |
| **API Endpoint** | `POST /api/v1/products` |
| **Request Content-Type** | `multipart/form-data` (for images) or `application/json` |
| **Pre-Submission Validation** | Full DTO validation (class-validator), merchant license status check |
| **Processing Steps** | 1. Validate JWT token and merchant role. 2. **Check merchant license status. If status is `pending`, return 403 Forbidden with error code `MERCHANT_NOT_APPROVED`.** 3. Verify merchant has approved shop. 4. Validate all required fields. 5. Generate slug from product name. 6. Check slug uniqueness. 7. Upload images (if provided). 8. Create product record in DB. 9. Invalidate product list cache. 10. Return created product DTO. |
| **Success Response** | 201 Created with product data |
| **Error Response (Pending License)** | 403 Forbidden — "Your account is pending approval. Product management is not available at this time." |
| **Post-Action** | Redirect to merchant product list |

### 6.2 Operation: Update Product (Merchant)

| Attribute | Specification |
|-----------|---------------|
| **Trigger** | Submit product form on `/merchant/products/:id/edit` |
| **API Endpoint** | `PATCH /api/v1/products/:id` |
| **Request Content-Type** | `multipart/form-data` or `application/json` |
| **Pre-Submission Validation** | Full DTO validation, product ownership check, merchant license status check |
| **Processing Steps** | 1. Validate JWT token and merchant role. 2. **Check merchant license status. If status is `pending`, return 403 Forbidden with error code `MERCHANT_NOT_APPROVED`.** 3. Verify product belongs to merchant. 4. Validate all fields. 5. Update product record. 6. Handle image additions/removals. 7. Invalidate product cache and list cache. 8. Return updated product DTO. |
| **Success Response** | 200 OK with updated product data |
| **Error Response (Pending License)** | 403 Forbidden — "Your account is pending approval. Product management is not available at this time." |
| **Post-Action** | Stay on edit page or redirect to list |

### 6.3 Operation: Delete Product (Soft Delete) (Merchant)

| Attribute | Specification |
|-----------|---------------|
| **Trigger** | Click delete button on merchant product list |
| **API Endpoint** | `DELETE /api/v1/products/:id` |
| **Request Content-Type** | None |
| **Pre-Submission Validation** | Product ownership check, merchant license status check |
| **Processing Steps** | 1. Validate JWT token and merchant role. 2. **Check merchant license status. If status is `pending`, return 403 Forbidden with error code `MERCHANT_NOT_APPROVED`.** 3. Verify product belongs to merchant. 4. **Check for active orders: Query `order_items` for this product where linked order status NOT IN ('delivered', 'cancelled').** 5. **If active orders exist, return 409 Conflict with error message.** 6. Set `is_active = false` (soft delete). 7. Invalidate product cache and list cache. 8. Return 204 No Content. |
| **Success Response** | 204 No Content |
| **Error Response (Pending License)** | 403 Forbidden — "Your account is pending approval. Product management is not available at this time." |
| **Error Response (Active Orders)** | 409 Conflict — "Cannot delete product with active orders" |
| **Post-Action** | Remove product from list view, show success toast |

### 6.4 Operation: Update Stock (Merchant)

| Attribute | Specification |
|-----------|---------------|
| **Trigger** | Update stock quantity from merchant product list or edit form |
| **API Endpoint** | `PATCH /api/v1/products/:id/stock` |
| **Request Content-Type** | `application/json` |
| **Pre-Submission Validation** | Stock quantity ≥ 0, product ownership, merchant license status check |
| **Processing Steps** | 1. Validate JWT token and merchant role. 2. **Check merchant license status. If status is `pending`, return 403 Forbidden with error code `MERCHANT_NOT_APPROVED`.** 3. Verify product belongs to merchant. 4. Validate stock quantity ≥ 0. 5. Update stock atomically. 6. Check low stock threshold. 7. Invalidate product cache. 8. Return updated stock info. |
| **Success Response** | 200 OK with stock data and low stock warning if applicable |
| **Error Response (Pending License)** | 403 Forbidden — "Your account is pending approval. Product management is not available at this time." |

### 6.5 Operation: Toggle Product Active/Inactive (Merchant)

| Attribute | Specification |
|-----------|---------------|
| **Trigger** | Toggle switch on merchant product list |
| **API Endpoint** | `PATCH /api/v1/products/:id` |
| **Request Content-Type** | `application/json` |
| **Pre-Submission Validation** | Product ownership check, merchant license status check |
| **Processing Steps** | 1. Validate JWT token and merchant role. 2. **Check merchant license status. If status is `pending`, return 403 Forbidden with error code `MERCHANT_NOT_APPROVED`.** 3. Verify product belongs to merchant. 4. Toggle `is_active` boolean. 5. Invalidate product cache and list cache. 6. Return updated product status. |
| **Success Response** | 200 OK with updated status |
| **Error Response (Pending License)** | 403 Forbidden — "Your account is pending approval. Product management is not available at this time." |

---

## 7. Input / Output Specification

### 7.1 Input Specification — Create Product (入力定義)

| Field | Display Name (EN) | Display Name (JA) | Data Type & Length | Required | Input Control | Validation |
|-------|-------------------|-------------------|-------------------|:--------:|---------------|------------|
| `name` | Product Name | 商品名 | VARCHAR(255) | Yes | Input (text) | `@IsString()`, `@IsNotEmpty()`, `@MaxLength(255)` |
| `shortDescription` | Short Description | 短い説明 | VARCHAR(500) | Yes | Textarea | `@IsString()`, `@IsNotEmpty()`, `@MaxLength(500)` |
| `description` | Description | 説明 | TEXT | Yes | Rich Text Editor | `@IsString()`, `@IsNotEmpty()` |
| `categoryId` | Category | カテゴリ | VARCHAR(25) | Yes | Select (tree) | `@IsString()`, `@IsNotEmpty()` |
| `price` | Price | 価格 | NUMERIC(10,2) | Yes | Input (number) | `@IsNumber()`, `@Min(0.01)` |
| `compareAtPrice` | Compare At Price | 比較価格 | NUMERIC(10,2) | No | Input (number) | `@IsOptional()`, `@IsNumber()`, `@Min(0)` |
| `sku` | SKU | SKU | VARCHAR(100) | No | Input (text) | `@IsOptional()`, `@IsString()`, `@MaxLength(100)` |
| `stockQuantity` | Stock Quantity | 在庫数 | INTEGER | Yes | Input (number) | `@IsInt()`, `@Min(0)`, default 0 |
| `lowStockThreshold` | Low Stock Threshold | 低在庫閾値 | INTEGER | No | Input (number) | `@IsInt()`, `@Min(0)`, default 10 |
| `skinTypes` | Skin Types | 肌タイプ | TEXT[] | No | Checkbox Group | `@IsArray()`, `@IsIn(['dry','oily','combination','sensitive','normal'])` |
| `ingredients` | Ingredients | 成分 | TEXT[] | No | Tag Input | `@IsArray()`, `@IsString({ each: true })` |
| `tags` | Tags | タグ | TEXT[] | No | Tag Input | `@IsArray()`, `@IsString({ each: true })` |
| `isActive` | Active | 有効フラグ | BOOLEAN | Yes | Switch | `@IsBoolean()`, default true |
| `isFeatured` | Featured | おすすめフラグ | BOOLEAN | No | Switch | `@IsBoolean()`, default false |
| `images` | Images | 画像 | File[] | Yes | File Upload | Max 10 files, 5MB each, JPG/PNG/WebP |

### 7.2 Input Specification — Update Product (入力定義)

Same as Create Product, with all fields optional (partial update).

### 7.3 Input Specification — Update Stock (入力定義)

| Field | Display Name (EN) | Display Name (JA) | Data Type & Length | Required | Input Control | Validation |
|-------|-------------------|-------------------|-------------------|:--------:|---------------|------------|
| `stockQuantity` | Stock Quantity | 在庫数 | INTEGER | Yes | Input (number) | `@IsInt()`, `@Min(0)` |

---

## 8. Input Validation Rules

### 8.1 Product Creation Validation (Strict Mode)

| Field | Validation Rule | Error Message (EN) | Error Message (JA) |
|-------|-----------------|--------------------|--------------------|
| `name` | Required, 1-255 chars | "Product name is required" / "Product name must not exceed 255 characters" | "商品名は必須です" / "商品名は255文字以内で入力してください" |
| `shortDescription` | Required, 1-500 chars | "Short description is required" / "Short description must not exceed 500 characters" | "短い説明は必須です" / "短い説明は500文字以内で入力してください" |
| `description` | Required, non-empty | "Description is required" | "説明は必須です" |
| `categoryId` | Required, valid CUID | "Category is required" / "Invalid category" | "カテゴリは必須です" / "無効なカテゴリです" |
| `price` | Required, > 0 | "Price is required" / "Price must be greater than 0" | "価格は必須です" / "価格は0より大きい値を入力してください" |
| `compareAtPrice` | Optional, > price | "Compare price must be greater than selling price" | "比較価格は販売価格より大きい値を入力してください" |
| `sku` | Optional, unique, max 100 chars | "SKU already exists" / "SKU must not exceed 100 characters" | "SKUは既に存在します" / "SKUは100文字以内で入力してください" |
| `stockQuantity` | Required, ≥ 0 | "Stock quantity must be 0 or greater" | "在庫数は0以上を入力してください" |
| `images` | Required, max 10 files, 5MB each | "At least one image is required" / "Maximum 10 images allowed" / "Image must not exceed 5MB" | "画像は最低1枚必要です" / "画像は最大10枚までです" / "画像は5MB以内にしてください" |

### 8.2 Validation Enforcement Layers

1. **Frontend (Client)**: React Hook Form + Zod schema validation with real-time feedback.
2. **Backend (Server)**: NestJS ValidationPipe + class-validator DTOs on all endpoints.
3. **Database (PostgreSQL)**: CHECK constraints and UNIQUE constraints as final guard.

---

## 9. Error Handling Specification

### 9.1 Error Response Structure

```json
{
  "statusCode": 400,
  "message": ["Product name is required", "Price must be greater than 0"],
  "error": "Bad Request",
  "timestamp": "2026-08-05T12:00:00.000Z",
  "path": "/api/v1/products"
}
```

### 9.2 Error Classification Table — Product CRUD

| HTTP Status | Error Code | Scenario | User-Facing Behavior |
|-------------|------------|----------|---------------------|
| `400` | `BAD_REQUEST` | Validation failures | Field-level inline errors + top banner |
| `401` | `UNAUTHORIZED` | Missing or invalid JWT | Redirect to login |
| `403` | `FORBIDDEN` | Not merchant/admin or not product owner | "You don't have permission to manage this product" |
| `403` | `MERCHANT_NOT_APPROVED` | Merchant license status is `pending` — all CRUD operations restricted | Error toast: "Your account is pending approval. Product management is not available at this time." + automatic redirect to home page (`/`) |
| `404` | `NOT_FOUND` | Product not found | "Product not found" with back link |
| `409` | `CONFLICT` | Slug or SKU already exists | "A product with this name/SKU already exists" |
| `409` | `CONFLICT` | Product has active orders (not in 'delivered' or 'cancelled' status) | "Cannot delete product with active orders. All orders must be completed first." |
| `413` | `PAYLOAD_TOO_LARGE` | Image file > 5MB | "Image file must not exceed 5MB" |
| `415` | `UNSUPPORTED_MEDIA_TYPE` | Invalid image format | "Only JPG, PNG, and WebP images are supported" |
| `429` | `TOO_MANY_REQUESTS` | Rate limit exceeded | "Too many requests. Please wait {seconds} seconds" |
| `500` | `INTERNAL_SERVER_ERROR` | Server error | "Something went wrong. Please try again" |

### 9.3 Frontend Error Display Behavior

- **Field-Level Validation**: Red border and inline text below invalid input.
- **Form-Level Summary**: Alert banner at top of form listing all errors.
- **Toast Notifications**: Used for API errors and successful actions.
- **Loading States**: Spinner on submit buttons during API calls.

---

## 10. Permission and Access Control

### 10.1 Authentication Requirements

- JSON Web Token (JWT) Bearer Token passed via `Authorization` header for merchant operations.

### 10.2 Protected Endpoints

| Endpoint | Access Level | Description |
|----------|-------------|-------------|
| `POST /products` | Protected (Merchant/Admin) | Create new product |
| `PATCH /products/:id` | Protected (Merchant/Admin) | Update product |
| `DELETE /products/:id` | Protected (Merchant/Admin) | Soft delete product |
| `PATCH /products/:id/stock` | Protected (Merchant/Admin) | Update stock |

### 10.3 Role-Based Access

| Role | Create Products | Edit Products | Delete Products | Manage Stock |
|------|:---------------:|:-------------:|:---------------:|:------------:|
| `merchant` | ✓ (own shop, approved license status) | ✓ (own products, approved license status) | ✓ (own products, approved license status) | ✓ (own products, approved license status) |

### 10.4 License Status Enforcement

Merchants with `pending` license status are restricted from all product management operations. The following enforcement applies:

**Backend (API Guard):**
```typescript
// Backend license status check — applied to all product CRUD endpoints
const merchant = await this.prisma.merchant.findUnique({ where: { userId: user.id } });
if (!merchant) throw new ForbiddenException('Merchant profile not found');
if (merchant.licenseStatus === 'pending') {
  throw new ForbiddenException({
    statusCode: 403,
    error: 'MERCHANT_NOT_APPROVED',
    message: 'Your account is pending approval. Product management is not available at this time.'
  });
}
```

**Frontend (Route Guard):**
```typescript
// Frontend route guard — applied to /merchant/products/* routes
const merchantProfile = await fetchMerchantProfile(user.id);
if (merchantProfile.licenseStatus === 'pending') {
  toast.error('Your account is pending approval. Product management is not available at this time.');
  router.push('/');
  return;
}
```

### 10.5 Ownership Enforcement

```typescript
// Backend ownership check example
const product = await this.prisma.product.findUnique({ where: { id } });
if (!product) throw new NotFoundException('Product not found');
if (user.role === 'merchant' && product.merchantId !== user.id) {
  throw new ForbiddenException('You can only manage your own products');
}

// Active order check for deletion
const activeOrders = await this.prisma.orderItem.findFirst({
  where: {
    productId: id,
    order: {
      status: { notIn: ['delivered', 'cancelled'] }
    }
  }
});
if (activeOrders) {
  throw new ConflictException('Cannot delete product with active orders');
}
```

### 10.6 Security Audit Logging

| Event | Data Logged | Retention |
|-------|-------------|-----------|
| `PRODUCT_CREATED` | merchantId, productId, timestamp | 90 days |
| `PRODUCT_UPDATED` | merchantId, productId, changes, timestamp | 90 days |
| `PRODUCT_DELETED` | merchantId, productId, timestamp | 90 days |
| `PRODUCT_IMAGE_UPLOADED` | merchantId, productId, fileSize, timestamp | 30 days |
| `STOCK_UPDATED` | merchantId, productId, oldQty, newQty, timestamp | 90 days |

---

## 11. Real-Time Notification Behavior

### 11.1 Low Stock Notifications

| Event | Trigger | Action |
|-------|---------|--------|
| `LOW_STOCK_WARNING` | Stock quantity ≤ lowStockThreshold | Display warning badge on merchant product list |
| `OUT_OF_STOCK` | Stock quantity = 0 | Display "Out of Stock" badge on merchant product list |

### 11.2 Product Status Changes

| Event | Trigger | Action |
|-------|---------|--------|
| `PRODUCT_DEACTIVATED` | Merchant toggles is_active to false | Product hidden from public view |
| `PRODUCT_FEATURED` | Merchant toggles is_featured to true | Product added to home page featured section |

---

## 12. Screen Transition Specification

### 12.1 Inbound Navigation

| Source | Target | Condition |
|--------|--------|-----------|
| Merchant dashboard | `/merchant/products` | Click "Products" menu |
| Merchant product list | `/merchant/products/new` | Click "Add New Product" |
| Merchant product list | `/merchant/products/:id/edit` | Click "Edit" button |

### 12.2 Internal Navigation

| Source | Target | Trigger |
|--------|--------|---------|
| `/merchant/products` | `/merchant/products/new` | Click "Add New" button |
| `/merchant/products` | `/merchant/products/:id/edit` | Click "Edit" button |
| `/merchant/products/new` | `/merchant/products` | Click "Cancel" or save success |
| `/merchant/products/:id/edit` | `/merchant/products` | Click "Cancel" or save success |

### 12.3 Outbound Navigation

| Source | Target | Condition |
|--------|--------|-----------|
| `/merchant/products/new` | `/merchant/products` | Product created successfully |
| `/merchant/products/:id/edit` | `/merchant/products` | Product updated successfully |

### 12.4 Error Navigation

| Source | Target | Condition |
|--------|--------|-----------|
| Any merchant product page | `/login` | 401 Unauthorized |
| Any merchant product page | `/unauthorized` | 403 Forbidden |
| Any merchant product page | `/` (Home) | 403 `MERCHANT_NOT_APPROVED` — error toast displayed before redirect |

---

## 13. Non-Functional Considerations

### 13.1 Performance Requirements

| Metric | Target |
|--------|--------|
| Image Upload (5MB) | ≤ 3 seconds |
| Product Create API | ≤ 1 second |
| Product Update API | ≤ 500 milliseconds |

### 13.2 Caching Strategy

| Cache Key | TTL | Invalidation Trigger |
|-----------|-----|---------------------|
| `cache:product:{slug}` | 5 minutes | Product update/delete |
| `cache:products:list:{hash}` | 2 minutes | Any product mutation |

### 13.3 Image Optimization

| Requirement | Description |
|-------------|-------------|
| Thumbnail Generation | Generate 100x100, 300x300, 600x600 variants |
| Format | Serve WebP when supported, fallback to JPEG |
| CDN Ready | Image URLs should be CDN-compatible for production |

### 13.4 Responsive Design Requirements

| Breakpoint | Layout |
|------------|--------|
| Desktop (≥ 1024px) | Full-width product form, sidebar navigation |
| Tablet (768px – 1023px) | Full-width product form, collapsible sidebar |
| Mobile (< 768px) | Stacked form layout, bottom navigation |

### 13.5 Accessibility Requirements

| Requirement | Implementation |
|-------------|---------------|
| WCAG 2.1 AA | Semantic HTML, ARIA labels on all interactive elements |
| Keyboard Navigation | Tab order through form fields, buttons, switches |
| Screen Reader | Alt text for product images, ARIA labels for actions |
| Color Contrast | Minimum 4.5:1 for text, 3:1 for UI components |
| Focus Indicators | Visible focus ring on all interactive elements |

---

## 14. Configurable Items (External Definitions)

Defined via `.env` configuration:

| Definition Key | Default Value | Description |
|----------------|---------------|-------------|
| `PRODUCT_IMAGES_MAX` | `10` | Maximum images per product |
| `PRODUCT_IMAGE_MAX_SIZE_MB` | `5` | Maximum file size per image in MB |
| `PRODUCT_LOW_STOCK_THRESHOLD` | `10` | Default low stock warning threshold |
| `PRODUCT_CACHE_TTL_SECONDS` | `300` | Product detail cache TTL (5 min) |
| `PRODUCT_LIST_CACHE_TTL_SECONDS` | `120` | Product list cache TTL (2 min) |

---

## 15. Cross-Reference Traceability Matrix

### 15.1 Requirements Definition Traceability

| Requirement ID | Requirement Description | Covered By (This Document) |
|----------------|-------------------------|----------------------------|
| M-PROD-001 | Merchant can create new products | UC-PROD-001, Sec 6.1 |
| M-PROD-002 | Merchant can edit existing products | UC-PROD-002, Sec 6.2 |
| M-PROD-003 | Merchant can delete products (soft delete) | UC-PROD-003, Sec 6.3 |
| M-PROD-004 | Merchant can upload product images | UC-PROD-004, BR-PROD-009~013 |
| M-PROD-005 | Merchant can manage inventory (stock quantity) | UC-PROD-005, Sec 6.4 |
| M-PROD-006 | Merchant can view own product list | UC-PROD-008, Sec 5.1 |
| M-PROD-007 | Merchant can toggle product active/inactive | UC-PROD-006, Sec 6.5 |
| M-PROD-008 | Merchant can set product as featured | UC-PROD-007, BR-PROD-017 |
| M-PROD-009 | Product creation requires: name, category, price, description | BR-PROD-001, Sec 8.1 |
| M-PROD-010 | Product images support JPG, PNG, WebP (max 5MB each) | BR-PROD-010~011, Sec 7.1 |
| M-PROD-011 | Product deletion restricted when active orders exist | BR-PROD-024~025, Sec 6.3 |
| M-PROD-012 | Merchant with pending account status restricted from all product CRUD operations | BR-PROD-026~027, Sec 6.1~6.5, Sec 10.4, Sec 12.4 |

### 15.2 Database Design Traceability

| Database Table | Relevant Functional Operations |
|----------------|-------------------------------|
| `products` | Create (INSERT), Update (SELECT+UPDATE), Delete (UPDATE is_active), List (SELECT+WHERE) |
| `categories` | Category tree (SELECT+JOIN), Filter by category (WHERE) |
| `reviews` | Product reviews (SELECT+WHERE), Avg rating calculation (aggregation) |
| `wishlists` | Wishlist toggle (INSERT/DELETE), Check wishlist status (SELECT) |
| `order_items` | Stock decrement (UPDATE), Sales history (SELECT) |
| `inventory_transactions` | Inventory change audit trail (INSERT), Stock history (SELECT+WHERE) |

### 15.3 Related Document References

| Document ID | Document Name | File Path |
|-------------|---------------|-----------|
| SKM-REQ-001 | Requirements Definition | `docs/core-work/要件定義書_REQUIREMENT_SPEC.md` |
| SKM-DBS-001 | Database Design Specification | `docs/core-work/データベース設計書_DATABASE_SPEC.md` |
| SKM-DEV-001 | Development Rules | `docs/core-work/開発ルール_DEVELOPMENT_RULES.md` |

---

## 16. Implementation Status & Gap Analysis

> **Documented:** 2026-08-20 | **Status:** As of codebase snapshot on 2026-08-20

### 16.1 Implementation Status Summary

| Module | Component | Status | Notes |
|--------|-----------|--------|-------|
| **Backend** | `src/modules/catalog/products/products.controller.ts` | Empty (0 lines) | All product controller logic unimplemented |
| | `src/modules/catalog/products/products.service.ts` | Empty (0 lines) | All product service logic unimplemented |
| | `src/modules/catalog/products/products.module.ts` | Empty (0 lines) | Module not registered in `AppModule` |
| | `src/modules/catalog/products/dto/create-product.dto.ts` | Empty (0 lines) | |
| | `src/modules/catalog/products/dto/update-product.dto.ts` | Empty (0 lines) | |
| | `src/modules/catalog/products/dto/product-query.dto.ts` | Empty (0 lines) | |
| | `src/modules/products/dto/delete-all-product.dto.ts` | **Implemented** (21 lines) | Orphaned — no service/controller consumes it |
| | `src/modules/products/dto/delete-all-response.dto.ts` | **Implemented** (21 lines) | Orphaned — no service/controller consumes it |
| | `src/common/guards/jwt-auth.guard.ts` | **Implemented** (39 lines) | |
| | `src/common/guards/roles.guard.ts` | **Implemented** (40 lines) | |
| | `src/modules/auth/guards/require-approved-merchant.guard.ts` | **Does not exist** | Required by BR-PROD-026~027, Sec 10.4 |
| | `src/shared/utils/slug.util.ts` | **Does not exist** | Required by DD_PROD_05 Sec 4 |
| **Frontend** | `pages/merchant/ProductManagement.tsx` | Empty (0 lines) | |
| | `features/merchant/products/components/ProductForm.tsx` | Empty (0 lines) | |
| | `features/merchant/products/hooks/useProducts.ts` | Empty (0 lines) | |
| | `features/merchant/products/services/product.service.ts` | Empty (0 lines) | |
| | `types/product.types.ts` | Empty (0 lines) | |
| | `i18n/locales/*/products.json` | Empty (0 lines) | EN, JA, MY all empty |
| **Tests** | `products.service.spec.ts` | **Does not exist** | 92 scenarios defined in DD_PROD_06 Sec 2.1 |
| | `products.controller.spec.ts` | **Does not exist** | 26 scenarios defined in DD_PROD_06 Sec 2.2 |
| | `require-approved-merchant.guard.spec.ts` | **Does not exist** | 5 scenarios defined in DD_PROD_06 Sec 2.3 |
| | E2E tests E2E-PROD-01~20 | **Does not exist** | 20 scenarios defined in DD_PROD_06 Sec 4 |

### 16.2 Use Case Implementation Traceability

| Use Case | Description | Backend | Frontend | Tests |
|----------|-------------|---------|----------|-------|
| UC-PROD-001 | Create new product | Not implemented | Not implemented | Not implemented |
| UC-PROD-002 | Edit existing product | Not implemented | Not implemented | Not implemented |
| UC-PROD-003 | Soft delete product | Not implemented | Not implemented | Not implemented |
| UC-PROD-004 | Upload product images | Not implemented | Not implemented | Not implemented |
| UC-PROD-005 | Update stock | Not implemented | Not implemented | Not implemented |
| UC-PROD-006 | Toggle active/inactive | Not implemented | Not implemented | Not implemented |
| UC-PROD-007 | Set featured | Not implemented | Not implemented | Not implemented |
| UC-PROD-008 | View product list | Not implemented | Not implemented | Not implemented |

### 16.3 Structural Issues Identified

| Issue | Severity | Description | Resolution |
|-------|----------|-------------|------------|
| Misplaced merchant module | Medium | `src/modules/merchant/products/` contains `MerchantsModule` (license approval), not product CRUD. This deviates from the project structure in Development Rules Sec 2.1. | Relocate `MerchantsModule` to `src/modules/admin/merchant-management/` and use `src/modules/catalog/products/` for actual product CRUD as designed. |
| Orphaned DTOs | Low | `DeleteAllProductsDto` and `DeleteAllResponseDto` exist in `src/modules/products/dto/` but are not consumed by any service or controller. | Move to `src/modules/catalog/products/dto/` when implementing UC-PROD-003. |
| Missing `RequireApprovedMerchantGuard` | High | Guard required by BR-PROD-026~027 to block pending/rejected merchants from product CRUD. Referenced in Sec 10.4 but file does not exist. | Implement at `src/modules/auth/guards/require-approved-merchant.guard.ts` per DD_PROD_03 Sec 3.1. |
| No product module in `AppModule` | High | `AppModule` imports Auth, Users, Merchants, AuditLogs, Admin — but no ProductsModule. Even if files had content, NestJS would not bootstrap the module. | Register `ProductsModule` in `AppModule` after implementing controller/service. |
| Missing slug utility | Medium | No `slug.util.ts` exists. Required by DD_PROD_05 Sec 4 for slug generation and uniqueness validation. | Implement at `src/shared/utils/slug.util.ts`. |

### 16.4 Implementation Priority Recommendation

Based on the functional spec and business value, recommended implementation order:

1. **Phase 1 — Core CRUD (MVP)**
   - `RequireApprovedMerchantGuard` (unblocks all merchant product operations)
   - Product Create (UC-PROD-001) with `CreateProductDto`
   - Product List (UC-PROD-008) with `ProductQueryDto`
   - Product Edit (UC-PROD-002) with `UpdateProductDto`
   - Product Soft Delete (UC-PROD-003) with active order guard
   - Register `ProductsModule` in `AppModule`
   - Frontend: `ProductManagement.tsx` page + `ProductForm.tsx` component

2. **Phase 2 — Inventory & Status**
   - Stock Update (UC-PROD-005) with `UpdateStockDto`
   - Toggle Active/Inactive (UC-PROD-006)
   - Set Featured (UC-PROD-007)
   - Inventory transaction logging

3. **Phase 3 — Images & Search**
   - Image Upload (UC-PROD-004)
   - Product search with filters
   - Category tree API

4. **Phase 4 — Advanced Operations**
   - Bulk activate/deactivate
   - Bulk soft delete
   - Delete all products
   - Frontend: Product cards, grid, detail view for buyers

5. **Phase 5 — Testing**
   - Unit tests (DD_PROD_06 Sec 2.1~2.3): 123 scenarios
   - E2E tests (DD_PROD_06 Sec 4): 20 scenarios

---

*End of Functional Specification (Product Management)*
