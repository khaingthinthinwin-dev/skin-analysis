{
  "name": "e2e-tests",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:debug": "npx playwright test --debug",
    "test:report": "npx playwright show-report",
    "update-pcl": "node ../scripts/update-pcl.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
